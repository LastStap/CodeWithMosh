# Щоденні завдання Java Backend Developer

Цей файл деталізує програму з `CourseProgramm.md`.

## Правила ведення файлу

- За один раз до файлу додаються завдання лише для **одного навчального дня**.
- Наступний день додається тільки після окремого запиту.
- Нові завдання мають спиратися на відповідний день із `CourseProgramm.md`.
- Перед додаванням наступного дня потрібно врахувати результат попереднього:
  виконані завдання, труднощі, помилки та теми для повторення.
- Не потрібно переходити до наступного дня, якщо не виконані критерії
  завершення поточного дня.
- Орієнтовна тривалість дня — **5 годин 30 хвилин чистого навчального часу**.
- Після кожних 50-60 хвилин роботи потрібно зробити перерву 10-15 хвилин.

---

# День 1. Середовище, перша Java-програма та Git

## Мета дня

Підготувати й перевірити робоче середовище, зрозуміти базову структуру
Java-програми, навчитися запускати її з IntelliJ IDEA та термінала, а також
перевірити базове розуміння Git.

Наприкінці дня ти повинен:

- знати встановлені версії Java, Maven і Git;
- розуміти призначення JDK, JVM, JRE, source code та bytecode;
- самостійно написати клас `HelloDeveloper`;
- скомпілювати й запустити програму з IntelliJ IDEA;
- скомпілювати й запустити програму з термінала;
- знайти створений `.class`-файл;
- розуміти базову структуру наявного Maven-проєкту;
- переглянути зміни через Git і підготувати змістовний commit.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Стартова діагностика | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Стартова діагностика — 30 хвилин

У перший день немає матеріалу за вчора, тому замість повторення потрібно
зафіксувати початковий рівень. Не користуйся пошуком, конспектом або AI під
час відповідей.

### Завдання 1.1. Дай письмові відповіді

Створи файл `learning-journal.md`, якщо його ще немає, і додай заголовок:

```markdown
# Learning Journal

## День 1. Стартова діагностика
```

Своїми словами дай короткі відповіді на запитання:

1. Що таке Java?
2. Чим Java відрізняється від JavaScript?
3. Що робить компілятор?
4. Що таке JVM?
5. Для чого потрібен метод `main`?
6. Чим вихідний файл `.java` відрізняється від `.class`?
7. Що таке Maven?
8. Що таке Git repository?
9. Чим `git add` відрізняється від `git commit`?
10. Як ти зазвичай шукаєш причину помилки в програмі?

Не виправляй відповіді одразу. Наприкінці дня повернися до них і доповни
іншим кольором у редакторі або окремим підрозділом `Уточнення після навчання`.

### Завдання 1.2. Самооцінка

Постав оцінку від 0 до 5:

- Java syntax;
- робота з IntelliJ IDEA;
- робота з терміналом;
- Maven;
- Git;
- читання повідомлень про помилки;
- технічна англійська.

Поруч із кожною оцінкою напиши одне речення, чому поставлена саме вона.

### Критерій завершення блоку

- Є відповіді на всі 10 запитань.
- Є 7 оцінок із коротким поясненням.
- Відповіді написані з пам'яті.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Getting Started**:

1. Setting Up the Development Environment.
2. Anatomy of a Java Program.
3. Your First Java Program.
4. How Java Code Gets Executed.
5. 5 Interesting Facts about Java.
6. Course Structure.

Пункти `Source Code`, `Follow Me Around` і `Learning Paths` достатньо
переглянути без детального конспектування.

### Як працювати з відео

1. Перед кожним уроком прочитай його назву та сформулюй одне запитання, на яке
   очікуєш отримати відповідь.
2. Під час першого перегляду не копіюй код механічно.
3. Після кожних 2-3 відео постав відтворення на паузу.
4. Закрий відео й запиши 3-5 ключових тез із пам'яті.
5. Повернися до відео та виправ лише фактичні помилки у своїх нотатках.
6. Усі приклади коду набирай вручну.

### Конспект

У `learning-journal.md` додай підрозділ:

```markdown
## День 1. Конспект курсу
```

Заповни таблицю власними словами:

| Поняття | Що це | Для чого потрібно |
|---|---|---|
| Source code |  |  |
| Compiler |  |  |
| Bytecode |  |  |
| JDK |  |  |
| JVM |  |  |
| JRE |  |  |
| `class` |  |  |
| `main` |  |  |
| `System.out.println` |  |  |

### Схема виконання Java-коду

Намалюй у конспекті та поясни кожен перехід:

```text
HelloDeveloper.java
        |
        | javac
        v
HelloDeveloper.class
        |
        | java / JVM
        v
Результат у консолі
```

Окремо письмово дай відповіді:

1. Чому `.class` не є звичайним текстовим Java-кодом?
2. Яку роль виконує JVM під час запуску?
3. Чому Java-програму можна запускати на різних операційних системах?
4. Чи входить компілятор `javac` до JVM?
5. Що станеться, якщо в програмі немає коректного методу `main`?

### Критерій завершення блоку

- Переглянуті всі заплановані уроки.
- Таблиця понять заповнена без копіювання визначень.
- Схема компіляції та запуску зрозуміла.
- Є письмові відповіді на 5 контрольних запитань.

---

## 3. Керована практика — 60 хвилин

### Завдання 3.1. Перевір інструменти

У терміналі, з кореня проєкту, виконай команди по черзі:

```bash
pwd
java -version
javac -version
mvn -version
git --version
git status
```

Запиши в `learning-journal.md`:

- поточну директорію;
- версію Java;
- версію `javac`;
- версію Maven;
- версію Git;
- назву активної Git-гілки;
- чи є в репозиторії незбережені зміни.

Важливо: у `pom.xml` зараз задано Java `25`. Порівняй це значення з версією,
яку показали `java -version` та `javac -version`. Сьогодні не змінюй
`pom.xml` лише заради збігу версій. Якщо версії несумісні, запиши це як
окреме спостереження.

### Завдання 3.2. Досліди Maven-проєкт

Знайди й відкрий:

- `pom.xml`;
- `src/main/java`;
- `src/test/java`;
- `target`.

У конспекті поясни призначення кожного елемента.

Потім знайди в `pom.xml`:

- `groupId`;
- `artifactId`;
- `version`;
- Java compiler source і target;
- test dependency.

Запиши їхні поточні значення. Поки що нічого в `pom.xml` не змінюй.

### Завдання 3.3. Повтори першу програму за курсом

В IntelliJ IDEA створи файл:

```text
src/main/java/daniil/dumshenko/HelloDeveloper.java
```

Початковий результат програми:

```text
Hello, Java Developer!
Day 1: Environment is ready.
```

Не копіюй готовий клас. Самостійно напиши:

- package declaration;
- class declaration;
- метод `main`;
- два виклики `System.out.println`.

Запусти клас через IntelliJ IDEA.

### Завдання 3.4. Зміни поведінку

Після успішного запуску:

1. Заміни текст першого рядка на привітання зі своїм ім'ям.
2. Додай третій рядок із метою навчання.
3. Навмисно видали одну крапку з комою.
4. Прочитай повідомлення компілятора й запиши його основну частину.
5. Виправ помилку та повторно запусти програму.

### Критерій завершення блоку

- Версії інструментів зафіксовані.
- Структура Maven-проєкту пояснена.
- `HelloDeveloper` запускається в IntelliJ IDEA.
- Одна syntax error створена, прочитана та виправлена свідомо.

---

## 4. Самостійне завдання — 120 хвилин

Цей блок виконай без перегляду рішення курсу. Документацію та власний
конспект використовувати можна. AI або готове рішення — лише після
щонайменше 30 хвилин самостійної спроби.

### Завдання 4.1. Розшир `HelloDeveloper`

Програма повинна вивести структуровану картку:

```text
================================
JAVA BACKEND LEARNING PROGRAM
================================
Student: <твоє ім'я>
Target role: Junior Java Backend Developer
Target country: Germany
Current day: 1 of 84
Today's topic: Java environment and execution
================================
```

Вимоги:

- кожен логічний рядок виводиться окремою командою;
- текст не повинен починатися або закінчуватися випадковими пробілами;
- результат має легко читатися в консолі;
- назва класу має точно збігатися з назвою файла;
- package має відповідати директорії.

### Завдання 4.2. Запусти програму з термінала через Maven

З кореня проєкту виконай:

```bash
mvn compile
```

Після успішної компіляції знайди:

```text
target/classes/daniil/dumshenko/HelloDeveloper.class
```

Запусти програму:

```bash
java -cp target/classes daniil.dumshenko.previousLessons.HelloDeveloper
```

Запиши:

- що створила команда `mvn compile`;
- навіщо використовується `-cp target/classes`;
- чому в команді запуску вказано повне ім'я
  `daniil.dumshenko.previousLessons.HelloDeveloper`.

### Завдання 4.3. Скомпілюй без Maven

Створи тимчасову директорію для результату компіляції:

```bash
mkdir -p /tmp/codewithmosh-day1
```

Скомпілюй клас:

```bash
javac -d /tmp/codewithmosh-day1 src/main/java/daniil/dumshenko/HelloDeveloper.java
```

Запусти його:

```bash
java -cp /tmp/codewithmosh-day1 daniil.dumshenko.previousLessons.HelloDeveloper
```

Порівняй:

- запуск через IntelliJ IDEA;
- компіляцію через Maven;
- ручну компіляцію через `javac`.

У конспекті напиши по 1-2 речення про те, що в кожному випадку відбувається
автоматично, а що ти робиш вручну.

### Завдання 4.4. Досліди bytecode

Переконайся, що `.class`-файл існує:

```bash
find target/classes -name 'HelloDeveloper.class'
```

Переглянь структуру скомпільованого класу:

```bash
javap -classpath target/classes daniil.dumshenko.previousLessons.HelloDeveloper
```

За бажанням переглянь інструкції bytecode:

```bash
javap -c -classpath target/classes daniil.dumshenko.previousLessons.HelloDeveloper
```

Не потрібно запам'ятовувати bytecode. Треба зрозуміти, що JVM виконує не
вихідний `.java`-файл безпосередньо.

Відповідай у конспекті:

1. Який файл є source code?
2. Який файл містить bytecode?
3. Яка команда компілює source code?
4. Яка команда запускає клас у JVM?
5. Чим Maven допоміг порівняно з ручним `javac`?

### Завдання 4.5. Перевір Git-стан

Виконай:

```bash
git status
git diff
git diff --stat
```

Зверни увагу: у репозиторії можуть уже бути зміни, які не стосуються Дня 1.
Не видаляй і не перезаписуй їх.

Визнач:

- які файли були змінені до початку сьогоднішньої роботи;
- які файли створені або змінені сьогодні;
- які саме рядки належать до сьогоднішнього завдання.

Підготуй для commit лише файли Дня 1:

```bash
git add TASKS.md learning-journal.md src/main/java/daniil/dumshenko/HelloDeveloper.java
git diff --cached
```

Уважно переглянь staged diff. Не додавай через `git add .` сторонні або
незавершені зміни.

Рекомендоване повідомлення commit:

```text
Complete day 1 Java environment exercises
```

Сам commit виконуй лише тоді, коли свідомо готовий зберегти ці зміни:

```bash
git commit -m "Complete day 1 Java environment exercises"
```

### Критерій завершення блоку

- Програма має весь необхідний вивід.
- Вона запускається через IntelliJ IDEA, Maven і ручні команди.
- `.class`-файл знайдений і переглянутий через `javap`.
- Різниця між трьома способами запуску пояснена.
- До commit підготовлені лише файли поточного дня.

---

## 5. Перевірка та рефакторинг — 30 хвилин

Автоматичні unit tests для `println` сьогодні не потрібні. Перевір результат
через запуск програми та аналіз коду.

### Завдання 5.1. Функціональна перевірка

Запусти програму й перевір:

1. Заголовок присутній.
2. Ім'я студента присутнє.
3. Цільова роль указана правильно.
4. Країна — Germany.
5. День — `1 of 84`.
6. Тема дня присутня.
7. Розділювачі мають однакову довжину.
8. Немає порожніх випадкових рядків.

### Завдання 5.2. Перевірка помилок

Навмисно виконай по одному разу, а потім виправ:

- запусти неправильне ім'я класу;
- прибери одну подвійні лапки в рядку;
- зміни регістр однієї літери в назві файла або класу.

Для кожного випадку запиши:

- команду або зміну;
- головний рядок помилки;
- причину;
- виправлення.

Не залишай навмисні помилки в кінцевому коді.

### Завдання 5.3. Рефакторинг

Переглянь `HelloDeveloper.java`:

- чи зрозуміла назва класу;
- чи правильний package;
- чи вирівняні відступи;
- чи немає закоментованого непотрібного коду;
- чи немає зайвих imports;
- чи кожен рядок виводу потрібний;
- чи немає орфографічних помилок.

Потім виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.HelloDeveloper
git diff --check
```

### Критерій завершення блоку

- Пройдені всі 8 функціональних перевірок.
- Досліджені 3 типові помилки.
- Після виправлень `mvn compile` завершується успішно.
- `git diff --check` не показує whitespace errors.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Повернися до стартової діагностики

Не видаляючи початкових відповідей, додай:

```markdown
## День 1. Уточнення після навчання
```

Повторно дай відповіді:

1. Що входить до JDK?
2. Яка різниця між JDK, JVM і JRE?
3. Як `.java` перетворюється на `.class`?
4. Що робить команда `java`?
5. Що Maven зробив за тебе під час `mvn compile`?

Порівняй відповіді зі стартовою діагностикою.

### Завдання 6.2. Заповни щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 1. Підсумок

### Що я сьогодні вивчив
- 
- 
- 

### Що я зробив самостійно
- 
- 

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що залишилося незрозумілим
- 

### Що потрібно повторити завтра
- 

### Фактичний час
- Стартова діагностика:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни вголос:

> Як код із файла `HelloDeveloper.java` перетворюється на результат у
> консолі та яку роль у цьому відіграють JDK, компілятор, bytecode і JVM?

Якщо пояснення переривається через прогалину, запиши цю прогалину до секції
`Що потрібно повторити завтра`.

---

## Фінальний checklist Дня 1

- [ ] Стартова діагностика виконана без підказок.
- [ ] Пройдено секцію Getting Started.
- [ ] Заповнена таблиця Java-понять.
- [ ] Перевірені Java, `javac`, Maven і Git.
- [ ] Досліджена структура Maven-проєкту.
- [ ] Створено `HelloDeveloper.java`.
- [ ] Програма запускається через IntelliJ IDEA.
- [ ] Програма компілюється через `mvn compile`.
- [ ] Програма запускається з `target/classes`.
- [ ] Програма скомпільована вручну через `javac`.
- [ ] `.class`-файл переглянутий через `javap`.
- [ ] Перевірено 8 вимог до консольного виводу.
- [ ] Досліджено й виправлено 3 навмисні помилки.
- [ ] Заповнений підсумок у `learning-journal.md`.
- [ ] Підготовлений або створений змістовний Git commit.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `HelloDeveloper` запускається трьома способами.
2. Ти можеш пояснити шлях `.java -> .class -> JVM -> результат`.
3. Усі обов'язкові пункти checklist виконані.
4. У Git не додані випадкові сторонні зміни.
5. У журналі записані труднощі, фактичний час і тема для повторення завтра.

Якщо хоча б один із перших чотирьох пунктів не виконаний, День 2 поки не
починай. Спочатку заверши або виправ завдання Дня 1.

---

# День 2. Змінні, primitive та reference types

## Допуск до Дня 2

Перед початком нового матеріалу закрий незавершені пункти Дня 1:

1. У `HelloDeveloper.java` зроби стандартну точку входу:

   ```java
   public static void main(String[] args)
   ```

2. Переконайся, що ім'я студента можна вивести зі `static main`. Обери
   найпростіший зрозумілий варіант і поясни, чому нестатичне поле не можна
   використати без створення об'єкта.
3. Виконай і запиши три перевірки помилок із завдання 5.2 Дня 1.
4. Додай підсумок Дня 1, фактичний час і тему для повторення.
5. Виконай:

   ```bash
   mvn compile
   java -cp target/classes daniil.dumshenko.previousLessons.HelloDeveloper
   git diff --check
   ```

Цей допуск не входить до 5 годин 30 хвилин Дня 2. Якщо будь-яка команда
завершується помилкою, спочатку знайди й виправ причину.

## Мета дня

Навчитися свідомо обирати тип змінної, розрізняти primitive та reference
types, використовувати константи, виконувати безпечне приведення типів і
передбачати результат операцій до запуску програми.

Наприкінці дня ти повинен:

- знати всі 8 primitive types та їхнє призначення;
- розуміти різницю між primitive value і reference;
- уміти оголошувати, ініціалізувати та змінювати змінні;
- використовувати `String`, escape sequences і `final`;
- розрізняти implicit та explicit casting;
- пояснювати overflow у `byte`;
- пояснювати, чому `double` не зберігає деякі десяткові дроби точно;
- самостійно написати й запустити `PersonalProfile`.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення і діагностика | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення і діагностика — 30 хвилин

### Завдання 1.1. Повтори День 1 без конспекту

У `learning-journal.md` додай:

```markdown
## День 2. Повторення
```

Письмово дай короткі відповіді:

1. Який шлях проходить `HelloDeveloper.java` до результату в консолі?
2. Чим JDK відрізняється від JVM?
3. Що створює `javac`?
4. Для чого команді `java` потрібен classpath?
5. Яка стандартна сигнатура точки входу Java-програми?

Не відкривай учорашній конспект, доки не запишеш усі відповіді. Після цього
перевір їх і познач виправлення окремо.

### Завдання 1.2. Початкова діагностика типів

До запуску коду передбач результат кожного фрагмента:

```java
int age = 25;
age = 26;
System.out.println(age);
```

```java
int result = 5 / 2;
System.out.println(result);
```

```java
double result = 5 / 2;
System.out.println(result);
```

```java
double result = 5.0 / 2;
System.out.println(result);
```

```java
char letter = 'A';
System.out.println(letter);
```

Для кожного прикладу запиши:

- прогноз;
- фактичний результат;
- пояснення, якщо прогноз був неправильним.

### Критерій завершення блоку

- Є 5 відповідей за матеріалом Дня 1.
- Є прогноз і фактичний результат для 5 фрагментів.
- Ти можеш пояснити різницю між `5 / 2` та `5.0 / 2`.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Types**, уроки 1-7.

Перед кожним уроком запиши одне запитання, на яке очікуєш отримати
відповідь. Після кожних 2-3 уроків закрий відео та відтвори 3-5 тез із
пам'яті. Приклади набирай вручну.

### Завдання 2.1. Таблиця primitive types

У `learning-journal.md` додай:

```markdown
## День 2. Конспект курсу
```

Заповни таблицю:

| Тип | Що зберігає | Приклад | Коли обрати |
|---|---|---|---|
| `byte` |  |  |  |
| `short` |  |  |  |
| `int` |  |  |  |
| `long` |  |  |  |
| `float` |  |  |  |
| `double` |  |  |  |
| `char` |  |  |  |
| `boolean` |  |  |  |

Числові межі не потрібно зубрити. Треба розуміти порядок розмірів і вміти
перевірити точні значення через `Byte.MIN_VALUE`, `Byte.MAX_VALUE` та
аналогічні константи wrapper classes.

### Завдання 2.2. Primitive і reference types

Своїми словами дай відповіді:

1. Що безпосередньо зберігає primitive-змінна?
2. Що концептуально зберігає reference-змінна?
3. Чому `String` не є primitive type?
4. Чим одинарні лапки відрізняються від подвійних?
5. Чому локальну змінну потрібно ініціалізувати перед читанням?
6. Що означає ключове слово `final`?
7. Чим declaration, initialization і assignment відрізняються між собою?

### Завдання 2.3. Літерали та escape sequences

Запиши по одному власному прикладу:

- `long` із суфіксом `L`;
- `float` із суфіксом `F`;
- читабельного числа з `_`;
- символу `char`;
- рядка з `\n`;
- рядка з `\t`;
- рядка з `\"`;
- Windows-шляху або іншого тексту з `\\`.

### Критерій завершення блоку

- Переглянуті уроки Types 1-7.
- Таблиця містить усі 8 primitive types.
- Є 7 письмових відповідей.
- Є 8 власних прикладів літералів та escape sequences.

---

## 3. Керована практика — 60 хвилин

Створи файл:

```text
src/main/java/daniil/dumshenko/TypeExperiments.java
```

Не копіюй усі приклади одним блоком. Додавай їх по одному, спочатку
передбачай результат, потім запускай і записуй висновок.

### Завдання 3.1. Десять присвоєнь

Створи щонайменше 10 прикладів, які разом показують:

1. declaration без assignment;
2. initialization;
3. повторний assignment;
4. присвоєння між двома `int`;
5. копіювання primitive value;
6. присвоєння одного `String` іншому;
7. конкатенацію `String`;
8. зміну `boolean`;
9. використання `char`;
10. використання `final`-константи.

Для прикладу копіювання primitive перевір:

```java
int first = 10;
int second = first;
first = 20;
```

До запуску запиши, яким буде `second` і чому.

### Завдання 3.2. П'ять прикладів casting

Реалізуй і поясни:

1. `int -> long`;
2. `int -> double`;
3. `double -> int`;
4. `int -> byte` у межах `byte`;
5. `int -> byte` поза межами `byte`.

Біля кожного прикладу в журналі вкажи:

- implicit чи explicit casting;
- прогнозований результат;
- фактичний результат;
- чи могла бути втрачена інформація.

### Завдання 3.3. Дві проблемні ситуації

**Overflow:**

```java
byte maximum = Byte.MAX_VALUE;
byte overflowed = (byte) (maximum + 1);
```

Перед запуском передбач значення `overflowed`, потім поясни результат.

**Точність `double`:**

```java
double sum = 0.1 + 0.2;
System.out.println(sum);
System.out.println(sum == 0.3);
```

Поясни, чому результат може відрізнятися від математичного очікування.
Запиши, чому для точних грошових обчислень не слід покладатися на `double`.
`BigDecimal` сьогодні використовувати не потрібно.

### Критерій завершення блоку

- `TypeExperiments` компілюється та запускається.
- Є 10 прикладів присвоєння.
- Є 5 прикладів casting із прогнозами.
- Overflow і проблема точності `double` відтворені та пояснені.

---

## 4. Самостійне завдання — 120 хвилин

Створи файл:

```text
src/main/java/daniil/dumshenko/PersonalProfile.java
```

Перші 45 хвилин працюй без AI та готового рішення. Можна використовувати
власний конспект і офіційну документацію Java.

### Завдання 4.1. Дані профілю

Програма повинна містити осмислені змінні для:

- імені;
- прізвища;
- віку;
- зросту в метрах;
- ваги в кілограмах;
- першої літери імені;
- статусу студента;
- кількості завершених навчальних днів;
- приблизної кількості годин навчання;
- цільової ролі;
- країни;
- улюбленої великої числової величини або населення міста/країни.

Використай у програмі всі 8 primitive types щонайменше по одному разу та
мінімум два значення типу `String`. Тип кожної змінної повинен мати
зрозуміле обґрунтування.

Не використовуй особисті дані, які не хочеш зберігати в Git.

### Завдання 4.2. Константи без magic numbers

Оголоси щонайменше три константи через `static final`, наприклад:

- кількість місяців у році;
- назву навчальної програми;
- загальну кількість днів програми;
- коефіцієнт для простого обчислення.

Не називай константи `NUMBER`, `VALUE` або `DATA`. Назва повинна пояснювати
значення.

### Завдання 4.3. Форматований вивід

Виведи профіль як читабельну картку. Обов'язково використай:

- `\n`;
- `\t`;
- лапки всередині рядка через `\"`;
- один зворотний слеш через `\\`;
- конкатенацію рядків;
- щонайменше одне просте обчислення зі змінними.

Приклад структури, але не готовий текст:

```text
================================
PERSONAL PROFILE
================================
Name: ...
Target role: ...
Learning progress:
    Completed days: ...
    Remaining days: ...
Quote: "..."
Example path: C:\...
================================
```

### Завдання 4.4. Пояснення вибору типів

У журналі створи таблицю:

| Змінна | Обраний тип | Чому цей тип |
|---|---|---|
|  |  |  |

Додай усі основні змінні `PersonalProfile`. Окремо відповідай:

1. Які змінні є primitive?
2. Які змінні є reference?
3. Які значення не повинні змінюватися?
4. Де в програмі можливе переповнення або втрата точності?
5. Чи є в коді число або текст, який краще зробити константою?

### Завдання 4.5. Запуск двома способами

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.PersonalProfile
```

Потім скомпілюй клас вручну:

```bash
mkdir -p /tmp/codewithmosh-day2
javac -d /tmp/codewithmosh-day2 src/main/java/daniil/dumshenko/PersonalProfile.java
java -cp /tmp/codewithmosh-day2 daniil.dumshenko.previousLessons.PersonalProfile
```

Обидва способи повинні давати однаковий результат.

### Критерій завершення блоку

- `PersonalProfile` використовує всі 8 primitive types і `String`.
- Є щонайменше 3 змістовні константи.
- Використані 4 заплановані escape sequences.
- У коді немає незрозумілих magic numbers.
- Вибір типів пояснений у журналі.
- Програма запускається через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Перевір `PersonalProfile`:

1. Усі обов'язкові поля присутні.
2. Усі 8 primitive types реально використовуються.
3. `String` використовується як reference type.
4. Константи оголошені через `static final`.
5. Назви констант написані у `UPPER_SNAKE_CASE`.
6. Результат простого обчислення правильний.
7. Escape sequences відображаються очікувано.
8. Вивід не містить випадкових пробілів і порожніх рядків.
9. Назви змінних пояснюють їхнє призначення.
10. Немає закоментованого непотрібного коду.

### Завдання 5.2. Перевірка compiler errors

По черзі створи, прочитай і виправ три помилки:

1. присвой `double` змінній `int` без explicit cast;
2. присвой текст у подвійних лапках змінній `char`;
3. спробуй повторно присвоїти значення `final`-змінній.

Для кожної запиши:

- зміну в коді;
- головний рядок compiler error;
- причину;
- виправлення.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Фінальні команди

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.TypeExperiments
java -cp target/classes daniil.dumshenko.previousLessons.PersonalProfile
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Виконані всі 10 функціональних перевірок.
- Досліджені та виправлені 3 compiler errors.
- Обидва класи запускаються після чистої компіляції.
- `git diff --check` не показує whitespace errors.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Назви 8 primitive types.
2. Чим primitive type відрізняється від reference type?
3. Чому `String` пишеться з великої літери?
4. Коли Java виконує implicit casting?
5. Чим explicit casting може бути небезпечним?
6. Чому `byte` може перейти з `127` до `-128`?
7. Чому `0.1 + 0.2` може не дорівнювати точно `0.3`?
8. Для чого використовують `final`?
9. Чому magic numbers ускладнюють підтримку коду?
10. Який тип ти обереш для грошей у майбутньому та чому не `double`?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 2. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і діагностика:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни:

> Як Java зберігає primitive та reference values, як відбувається casting і
> чому неправильний вибір числового типу може призвести до overflow або
> втрати точності?

Якщо пояснення переривається, запиши прогалину до тем на повторення.

---

## Фінальний checklist Дня 2

- [ ] Закритий допуск за День 1.
- [ ] Виконане повторення без конспекту.
- [ ] Пройдені уроки Types 1-7.
- [ ] Заповнена таблиця 8 primitive types.
- [ ] Пояснена різниця між primitive та reference types.
- [ ] Створено `TypeExperiments.java`.
- [ ] Виконано 10 прикладів присвоєння.
- [ ] Виконано 5 прикладів casting із прогнозами.
- [ ] Відтворено й пояснено overflow у `byte`.
- [ ] Відтворено й пояснено проблему точності `double`.
- [ ] Створено `PersonalProfile.java`.
- [ ] У `PersonalProfile` використані всі primitive types і `String`.
- [ ] Використано щонайменше 3 константи без magic numbers.
- [ ] Програма запускається через Maven і ручний `javac`.
- [ ] Виконані 10 функціональних перевірок.
- [ ] Досліджені та виправлені 3 compiler errors.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Зміни переглянуті через Git.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `TypeExperiments` і `PersonalProfile` компілюються та запускаються.
2. Ти можеш обґрунтувати тип кожної основної змінної.
3. Ти можеш пояснити casting, overflow і неточність `double`.
4. Усі обов'язкові пункти checklist виконані.
5. У журналі записані прогнози, помилки, фактичний час і тема для повторення.
6. У Git не підготовлені випадкові сторонні зміни.

---

# День 3. Масиви, вирази та `Math`

## Допуск до Дня 3

Перед початком нового матеріалу переконайся, що День 2 справді завершений:

1. `TypeExperiments.java` і `PersonalProfile.java` компілюються.
2. У `learning-journal.md` є підсумок Дня 2, фактичний час і тема для
   повторення.
3. Ти можеш без конспекту пояснити:
   - 8 primitive types;
   - різницю між primitive та reference types;
   - implicit і explicit casting;
   - overflow у `byte`;
   - чому `double` не підходить для точних грошових розрахунків.
4. Виконай:

   ```bash
   mvn compile
   java -cp target/classes daniil.dumshenko.previousLessons.TypeExperiments
   java -cp target/classes daniil.dumshenko.previousLessons.PersonalProfile
   git diff --check
   ```

Якщо будь-яка команда завершується помилкою, спочатку виправ День 2. Не
починай новий матеріал, поки не зрозумів причину помилки.

## Мета дня

Навчитися працювати з одновимірними та багатовимірними масивами, передбачати
результат арифметичних виразів, використовувати `Math`, округлення та
форматування чисел у практичних задачах.

Наприкінці дня ти повинен:

- створювати та ініціалізувати масиви;
- читати й змінювати елементи масиву за індексом;
- пояснювати, чому індексація починається з `0`;
- розуміти `ArrayIndexOutOfBoundsException`;
- створювати та виводити матрицю 3x3;
- самостійно рахувати `min`, `max`, `sum` і `average`;
- розвертати масив без готового методу;
- передбачати порядок виконання арифметичних операцій;
- використовувати `Math.round`, `Math.ceil`, `Math.floor`, `Math.max`,
  `Math.min` і `Math.random`;
- форматувати числа через `NumberFormat`;
- написати й запустити `ArrayStatistics` і `CurrencyConverter`.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення і діагностика | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення і діагностика — 30 хвилин

### Завдання 1.1. Повтори День 2 без конспекту

У `learning-journal.md` додай:

```markdown
## День 3. Повторення
```

Письмово дай короткі відповіді:

1. Назви 8 primitive types.
2. Чим primitive value відрізняється від reference value?
3. Що означає `final`?
4. Коли Java дозволяє implicit casting?
5. Чому explicit casting може втратити частину даних?
6. Що відбувається при `byte` overflow?
7. Чому `0.1 + 0.2` не завжди дає рівно `0.3`?

Спочатку відповідай із пам'яті. Потім перевір конспект і допиши уточнення
окремим підпунктом `Уточнення після перевірки`.

### Завдання 1.2. Передбач результат виразів

До запуску коду передбач результат кожного фрагмента:

```java
int result = 10 + 3 * 2;
System.out.println(result);
```

```java
int result = (10 + 3) * 2;
System.out.println(result);
```

```java
double result = 10 / 4;
System.out.println(result);
```

```java
double result = 10.0 / 4;
System.out.println(result);
```

```java
int x = 5;
int y = ++x * 2;
System.out.println(x);
System.out.println(y);
```

Для кожного прикладу запиши:

- прогноз;
- фактичний результат;
- пояснення, якщо прогноз був неправильним.

### Критерій завершення блоку

- Є 7 відповідей за матеріалом Дня 2.
- Є прогноз і фактичний результат для 5 фрагментів.
- Ти можеш пояснити різницю між `10 / 4` та `10.0 / 4`.
- Ти можеш пояснити роль дужок у порядку операцій.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Types**, уроки 8-15:

1. Arrays.
2. Multi-dimensional Arrays.
3. Constants.
4. Arithmetic Expressions.
5. Order of Operations.
6. Casting.
7. The Math Class.
8. Formatting Numbers.

Уроки про constants і casting сьогодні частково повторюють День 2. Під час
повтору не перемотуй автоматично: шукай деталі, які вчора були незрозумілі.

### Як працювати з відео

1. Перед кожним уроком запиши одне запитання.
2. Після уроків 8-9 зупинись і самостійно створи маленький масив.
3. Після уроків 11-13 зупинись і розв'яжи 3 вирази вручну.
4. Після уроків 14-15 закрий відео й напиши власний приклад із `Math` та
   форматуванням числа.
5. Усі приклади набирай вручну, не копіюй із відео.

### Завдання 2.1. Конспект про масиви

У `learning-journal.md` додай:

```markdown
## День 3. Конспект курсу
```

Своїми словами відповідай:

1. Що таке масив?
2. Чому масив має фіксований розмір?
3. Що означає індекс елемента?
4. Чому перший елемент має індекс `0`?
5. Що повертає `array.length`?
6. Що станеться при зверненні до неіснуючого індексу?
7. Чим `int[] numbers = new int[5]` відрізняється від
   `int[] numbers = {1, 2, 3, 4, 5}`?
8. Як у Java виглядає двовимірний масив?

### Завдання 2.2. Таблиця `Math` і форматування

Заповни таблицю:

| Інструмент | Що робить | Власний приклад | Результат |
|---|---|---|---|
| `Math.round` |  |  |  |
| `Math.ceil` |  |  |  |
| `Math.floor` |  |  |  |
| `Math.max` |  |  |  |
| `Math.min` |  |  |  |
| `Math.random` |  |  |  |
| `NumberFormat.getCurrencyInstance()` |  |  |  |
| `NumberFormat.getPercentInstance()` |  |  |  |

### Завдання 2.3. Порядок операцій

Розв'яжи письмово до запуску:

```java
int a = 10 + 2 * 3;
int b = (10 + 2) * 3;
int c = 20 / 4 + 6;
int d = 20 / (4 + 6);
double e = 5 + 2.0 * 4 / 3;
```

Для кожного виразу запиши:

- порядок виконання кроків;
- прогнозований результат;
- фактичний результат після запуску.

### Критерій завершення блоку

- Переглянуті уроки Types 8-15.
- Є 8 відповідей про масиви.
- Таблиця `Math` і `NumberFormat` заповнена.
- Є письмовий розбір 5 виразів на порядок операцій.

---

## 3. Керована практика — 60 хвилин

Створи файл:

```text
src/main/java/daniil/dumshenko/ArrayExperiments.java
```

Додавай приклади поступово. Перед запуском кожного блоку записуй прогноз у
журнал.

### Завдання 3.1. Одновимірні масиви

Реалізуй приклади:

1. Створи `int[] scores = {78, 92, 64, 100, 85}`.
2. Виведи перший, третій і останній елемент.
3. Зміни другий елемент на нове значення.
4. Виведи довжину масиву.
5. Виведи весь масив через `Arrays.toString`.
6. Створи `double[] prices` на 4 елементи через `new double[4]`.
7. Заповни `prices` вручну через індекси.
8. Виведи `prices` через `Arrays.toString`.

Письмово поясни, чому звичайний `System.out.println(scores)` не виводить
масив як список чисел.

### Завдання 3.2. Помилка індексу

Навмисно звернись до індексу, якого не існує, наприклад:

```java
System.out.println(scores[5]);
```

Запиши:

- повну назву exception;
- який індекс був неправильним;
- яка фактична довжина масиву;
- чому останній допустимий індекс дорівнює `length - 1`.

Після цього виправ код. У фінальній версії програма не повинна падати.

### Завдання 3.3. Матриця 3x3

Створи `int[][] matrix` із числами від 1 до 9:

```text
1 2 3
4 5 6
7 8 9
```

Виведи її двома способами:

1. Через `Arrays.deepToString(matrix)`.
2. Як три окремі рядки, щоб результат був схожий на матрицю.

Поясни:

- що означає `matrix.length`;
- що означає `matrix[0].length`;
- чим `Arrays.toString` відрізняється від `Arrays.deepToString` для
  двовимірного масиву.

### Завдання 3.4. `Math` на практиці

У тому ж класі створи приклади:

1. Знайди більше з двох чисел через `Math.max`.
2. Знайди менше з двох чисел через `Math.min`.
3. Округли `14.6` через `round`, `ceil` і `floor`.
4. Згенеруй випадкове число від `0` до `1`.
5. Згенеруй випадкове ціле число від `1` до `100`.

Для випадкового числа від `1` до `100` письмово поясни формулу.

### Критерій завершення блоку

- `ArrayExperiments` компілюється та запускається.
- Є приклади створення масиву через literal і через `new`.
- Є приклад читання, зміни та виводу елементів.
- `ArrayIndexOutOfBoundsException` свідомо створений, пояснений і виправлений.
- Матриця 3x3 виведена двома способами.
- Є робочі приклади `Math`.

---

## 4. Самостійне завдання — 120 хвилин

Перші 45 хвилин працюй без AI та готового рішення. Можна використовувати
власний конспект і офіційну документацію Java.

### Завдання 4.1. `ArrayStatistics`

Створи файл:

```text
src/main/java/daniil/dumshenko/ArrayStatistics.java
```

Програма повинна працювати з масивом:

```java
int[] transactions = {120, -45, 300, -80, 50, -20, 700};
```

Реалізуй без готових методів для статистики:

1. `sum` усіх елементів;
2. `min`;
3. `max`;
4. `average`;
5. кількість додатних значень;
6. кількість від'ємних значень;
7. окрему суму доходів;
8. окрему суму витрат;
9. копію масиву у зворотному порядку;
10. вивід оригінального та розвернутого масивів.

Важливо:

- `average` має бути `double`, не `int`;
- не використовуй `Collections`, streams або готовий reverse;
- для виводу масивів використовуй `Arrays.toString`;
- назви змінних мають пояснювати зміст, а не механіку.

Очікувані ідеї для перевірки:

- у масиві є і доходи, і витрати;
- витрати можна рахувати як суму від'ємних чисел або як абсолютну суму, але
  обраний варіант треба явно підписати у виводі;
- розвернутий масив не повинен змінювати оригінальний.

### Завдання 4.2. Перевір крайні випадки для `ArrayStatistics`

Тимчасово перевір програму на додаткових масивах:

```java
int[] onlyIncome = {100, 200, 300};
int[] onlyExpenses = {-10, -20, -30};
int[] singleValue = {42};
int[] withZero = {0, 100, -50};
```

Не обов'язково залишати всі ці масиви активними у фінальному коді. Але в
журналі запиши для кожного:

- прогноз `sum`, `min`, `max`, `average`;
- фактичний результат;
- чи правильно працює reverse.

Порожній масив сьогодні можна не підтримувати в коді, але письмово відповідай:

> Що станеться з `min`, `max` і `average`, якщо масив порожній?

### Завдання 4.3. `CurrencyConverter`

Створи файл:

```text
src/main/java/daniil/dumshenko/CurrencyConverter.java
```

Програма повинна конвертувати суму з EUR у USD, GBP і UAH.

Вимоги:

- сума в EUR зберігається у змінній;
- курси валют винесені в `static final` константи;
- результат рахується через арифметичні вирази;
- результат виводиться у форматі валюти через `NumberFormat`;
- додатково виведи один відсоток через `NumberFormat.getPercentInstance()`;
- використай `Math.round` або `Math.floor` в окремому прикладі округлення;
- не використовуй введення з клавіатури сьогодні.

Приклад структури виводу:

```text
================================
CURRENCY CONVERTER
================================
Base amount: ...
EUR to USD: ...
EUR to GBP: ...
EUR to UAH: ...
Example fee: ...
Rounded amount: ...
================================
```

Курси валют можуть бути навчальними, не актуальними ринковими даними. У
виводі або коментарі вкажи, що це `training rates`.

### Завдання 4.4. Пояснення до самостійної роботи

У `learning-journal.md` додай:

```markdown
## День 3. Самостійне завдання
```

Відповідай:

1. Чому для `average` потрібно хоча б одне `double`-значення в обчисленні?
2. Чому останній індекс масиву — це `length - 1`?
3. Як працює алгоритм розвороту масиву?
4. Чому оригінальний масив не повинен змінюватися під час створення reversed
   copy?
5. Які значення краще винести в константи в `CurrencyConverter`?
6. Чому `NumberFormat` краще за ручне додавання символу валюти до рядка?
7. Де в цих задачах може виникнути integer division?

### Завдання 4.5. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.ArrayExperiments
java -cp target/classes daniil.dumshenko.previousLessons.ArrayStatistics
java -cp target/classes daniil.dumshenko.previousLessons.CurrencyConverter
```

Потім скомпілюй два самостійні класи вручну:

```bash
mkdir -p /tmp/codewithmosh-day3
javac -d /tmp/codewithmosh-day3 src/main/java/daniil/dumshenko/ArrayStatistics.java src/main/java/daniil/dumshenko/CurrencyConverter.java
java -cp /tmp/codewithmosh-day3 daniil.dumshenko.previousLessons.ArrayStatistics
java -cp /tmp/codewithmosh-day3 daniil.dumshenko.previousLessons.CurrencyConverter
```

### Критерій завершення блоку

- `ArrayStatistics` рахує `sum`, `min`, `max`, `average`.
- Масив розвертається без готового reverse.
- Оригінальний масив не змінюється під час розвороту.
- Перевірені щонайменше 4 крайні випадки.
- `CurrencyConverter` використовує константи для курсів.
- Валюта й відсоток форматуються через `NumberFormat`.
- Обидві програми запускаються через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Перевір `ArrayStatistics`:

1. `sum` правильний.
2. `min` правильний.
3. `max` правильний.
4. `average` не обрізається до `int`.
5. Додатні та від'ємні значення рахуються правильно.
6. Доходи та витрати підписані зрозуміло.
7. Reversed copy має правильний порядок.
8. Оригінальний масив після reverse не змінився.

Перевір `CurrencyConverter`:

1. Курси валют оголошені як `static final`.
2. У виводі зрозуміло, яка сума базова.
3. USD, GBP і UAH рахуються окремо.
4. Валютні значення форматуються через `NumberFormat`.
5. Відсоток форматуються через `NumberFormat`.
6. Є приклад округлення через `Math`.
7. У виводі немає випадкових пробілів і порожніх рядків.

### Завдання 5.2. Перевірка помилок

По черзі створи, прочитай і виправ три помилки:

1. звернення до індексу за межами масиву;
2. спроба змінити значення `static final` константи;
3. неправильний import або відсутній import для `Arrays` чи `NumberFormat`.

Для кожної запиши:

- зміну в коді;
- головний рядок помилки або exception;
- причину;
- виправлення.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Рефакторинг

Переглянь код:

- чи немає дублювання однакових чисел, які треба винести в константи;
- чи зрозумілі назви змінних;
- чи немає закоментованого непотрібного коду;
- чи imports справді використовуються;
- чи форматування однакове в усіх класах;
- чи довгі рядки можна розбити без втрати читабельності;
- чи всі результати у виводі підписані так, щоб не треба було вгадувати.

Потім виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.ArrayExperiments
java -cp target/classes daniil.dumshenko.previousLessons.ArrayStatistics
java -cp target/classes daniil.dumshenko.previousLessons.CurrencyConverter
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Виконані всі функціональні перевірки.
- Досліджені та виправлені 3 помилки.
- Усі три класи запускаються після чистої компіляції.
- `git diff --check` не показує whitespace errors.
- У Git видно лише очікувані зміни.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Що таке масив?
2. Чому масив має фіксований розмір?
3. Чому перший індекс дорівнює `0`?
4. Що означає `array.length`?
5. Чому останній індекс — `array.length - 1`?
6. Коли виникає `ArrayIndexOutOfBoundsException`?
7. Як створити двовимірний масив?
8. Чим `Arrays.toString` відрізняється від `Arrays.deepToString`?
9. Як вручну знайти `min` і `max` у масиві?
10. Як розвернути масив без готового методу?
11. Чому `average` може бути неправильним при integer division?
12. Для чого використовують `Math.round`, `ceil` і `floor`?
13. Для чого використовують `NumberFormat`?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 3. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і діагностика:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни:

> Як у Java працюють масиви, чому індекси починаються з нуля, як вручну
> порахувати статистику масиву та чому форматування чисел краще робити через
> спеціальні інструменти, а не через ручну конкатенацію рядків?

Якщо пояснення переривається, запиши прогалину до тем на повторення.

---

## Фінальний checklist Дня 3

- [ ] Закритий допуск за День 2.
- [ ] Виконане повторення без конспекту.
- [ ] Пройдені уроки Types 8-15.
- [ ] Заповнені відповіді про масиви.
- [ ] Заповнена таблиця `Math` і `NumberFormat`.
- [ ] Розв'язано 5 виразів на порядок операцій.
- [ ] Створено `ArrayExperiments.java`.
- [ ] Створено та змінено одновимірний масив.
- [ ] Свідомо створено, пояснено й виправлено `ArrayIndexOutOfBoundsException`.
- [ ] Створено та виведено матрицю 3x3.
- [ ] Виконані практичні приклади з `Math`.
- [ ] Створено `ArrayStatistics.java`.
- [ ] Пораховано `sum`, `min`, `max`, `average`.
- [ ] Пораховано доходи, витрати та кількість додатних/від'ємних значень.
- [ ] Масив розвернуто без готового методу.
- [ ] Перевірено 4 крайні випадки для статистики.
- [ ] Створено `CurrencyConverter.java`.
- [ ] Курси валют винесені в `static final` константи.
- [ ] Валюта й відсоток форматуються через `NumberFormat`.
- [ ] Усі класи запускаються через Maven.
- [ ] Самостійні класи запускаються через ручний `javac`.
- [ ] Досліджені та виправлені 3 помилки.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Зміни переглянуті через Git.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `ArrayExperiments`, `ArrayStatistics` і `CurrencyConverter` компілюються та
   запускаються.
2. Ти можеш пояснити індексацію масивів і причину `ArrayIndexOutOfBoundsException`.
3. Ти можеш вручну знайти `min`, `max`, `sum`, `average` і розвернути масив.
4. Ти можеш пояснити порядок операцій у виразах із дужками, множенням,
   діленням, додаванням і casting.
5. Ти розумієш базову різницю між округленням через `Math` і форматуванням
   через `NumberFormat`.
6. У журналі записані прогнози, помилки, фактичний час і тема для повторення.
7. У Git не підготовлені випадкові сторонні зміни.

---

# День 4. Введення даних і Mortgage Calculator

## Мета дня

Навчитися приймати дані від користувача через консоль, перетворювати введений
текст у числа, форматувати результат і реалізувати перший повноцінний
калькулятор із перевіркою некоректного вводу.

Наприкінці дня ти повинен:

- розуміти, для чого потрібен `Scanner`;
- вміти читати `int`, `double` і `String` із консолі;
- пояснити різницю між введенням даних і hardcoded значеннями;
- самостійно реалізувати `MortgageCalculator`;
- перевірити нульову ставку, від'ємні значення та великий строк;
- винести читання даних, формулу та форматування в окремі методи;
- пояснити формулу щомісячного платежу своїми словами;
- запустити програму через Maven і напряму через `java`.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення та допуск | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення та допуск — 30 хвилин

### Завдання 1.1. Перевір готовність після Дня 3

Перед новою темою перевір, що День 3 справді закритий:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.ArrayExperiments
java -cp target/classes daniil.dumshenko.previousLessons.ArrayStatistics
java -cp target/classes daniil.dumshenko.previousLessons.CurrencyConverter
git status
```

Якщо будь-яка програма не компілюється або падає, спочатку виправ День 3.
Не переходь до Mortgage Calculator, поки базові класи не запускаються.

### Завдання 1.2. Повторення без конспекту

У `learning-journal.md` додай підрозділ:

```markdown
## День 4. Повторення Дня 3
```

Без конспекту письмово відповідай:

1. Як створити масив `int` із 5 елементів?
2. Чому останній індекс дорівнює `length - 1`?
3. Як вручну знайти `min` у масиві?
4. Як порахувати `average`, щоб не отримати integer division?
5. Чим `Arrays.toString` відрізняється від `Arrays.deepToString`?
6. Для чого потрібен `NumberFormat.getCurrencyInstance()`?
7. Чим форматування числа відрізняється від математичного округлення?

Після відповідей відкрий код Дня 3 і допиши короткі уточнення, якщо щось
згадав неправильно.

### Завдання 1.3. Прогноз перед новою темою

Перед переглядом курсу запиши відповіді:

1. Як, на твою думку, Java читає введення користувача з консолі?
2. Що може піти не так, якщо користувач введе текст замість числа?
3. Чому калькулятор іпотеки не повинен працювати тільки з hardcoded числами?
4. Які дані потрібні для розрахунку щомісячного платежу?

### Критерій завершення блоку

- Усі класи Дня 3 компілюються та запускаються.
- Є відповіді на 7 питань повторення.
- Є 4 прогнози перед темою `Scanner` і Mortgage Calculator.
- У `git status` немає неочікуваних сторонніх змін.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Types**:

1. Reading Input.
2. Project: Mortgage Calculator.
3. Solution: Mortgage Calculator.
4. Summary.

Якщо у твоїй версії курсу назви трохи відрізняються, орієнтуйся на уроки про
`Scanner`, введення даних і проєкт Mortgage Calculator.

### Як працювати з відео

1. Перед уроком `Reading Input` запиши, які методи читання очікуєш побачити.
2. Під час першого перегляду не копіюй код повністю.
3. Після пояснення `Scanner` зупини відео й самостійно відтвори мінімальний
   приклад читання числа та рядка.
4. Перед уроком з рішенням Mortgage Calculator закрий відео й спробуй
   реалізувати власну версію.
5. Після перегляду рішення Mosh не замінюй свій код механічно. Спочатку
   запиши, які відмінності мають сенс.

### Конспект

У `learning-journal.md` додай підрозділ:

```markdown
## День 4. Конспект курсу
```

Заповни таблицю:

| Поняття | Що це | Типова помилка |
|---|---|---|
| `Scanner` |  |  |
| `System.in` |  |  |
| `nextInt()` |  |  |
| `nextDouble()` |  |  |
| `nextLine()` |  |  |
| Principal |  |  |
| Annual interest rate |  |  |
| Monthly interest rate |  |  |
| Period in years |  |  |
| Number of payments |  |  |

Окремо письмово поясни:

1. Чому річну ставку потрібно перетворити на місячну?
2. Чому строк у роках потрібно перетворити на кількість місяців?
3. Які змінні у формулі можуть бути `int`, а які краще зробити `double`?
4. Чому результат платежу треба форматувати як валюту?
5. Що станеться, якщо користувач введе від'ємну principal amount?

### Формула

Запиши формулу своїми словами та підпиши кожну змінну:

```text
M = P * (r * (1 + r)^n) / ((1 + r)^n - 1)
```

Де:

- `M` — щомісячний платіж;
- `P` — сума кредиту;
- `r` — місячна відсоткова ставка;
- `n` — кількість платежів.

Не достатньо просто вставити формулу. Потрібно пояснити, які одиниці виміру має
кожне значення до розрахунку.

### Критерій завершення блоку

- Переглянуті уроки про введення даних і Mortgage Calculator.
- Є таблиця понять із типовими помилками.
- Формула записана й пояснена власними словами.
- Є відповіді на 5 контрольних запитань.

---

## 3. Керована практика — 60 хвилин

### Завдання 3.1. Створи клас для експериментів із вводом

Створи файл:

```text
src/main/java/daniil/dumshenko/InputExperiments.java
```

У програмі перевір:

1. читання цілого числа;
2. читання дробового числа;
3. читання одного слова;
4. читання повного рядка;
5. що відбувається, якщо після `nextInt()` одразу викликати `nextLine()`;
6. що відбувається, якщо користувач вводить текст там, де очікується число.

Для кожного експерименту запиши в `learning-journal.md`:

- що ти очікував побачити;
- що сталося після запуску;
- чи збігся прогноз;
- який висновок для Mortgage Calculator.

### Завдання 3.2. Досліди помилки вводу

Навмисно запусти `InputExperiments` із такими значеннями:

1. `1000`;
2. `1000.50`;
3. `hello`;
4. порожній рядок;
5. число з комою замість крапки, наприклад `10,5`.

Не виправляй усе одразу. Спочатку прочитай повідомлення про помилку або
поведінку програми. У журналі запиши:

- який input використав;
- що програма очікувала;
- що отримала;
- головний рядок exception, якщо він є;
- як це може вплинути на калькулятор.

### Завдання 3.3. Підготуй ручний приклад для формули

До написання Mortgage Calculator порахуй один приклад вручну або через
калькулятор:

- principal: `100000`;
- annual interest rate: `5`;
- period: `30` років.

Запиши:

- місячну ставку;
- кількість платежів;
- очікуваний щомісячний платіж приблизно;
- чому результат може трохи відрізнятися через округлення.

### Завдання 3.4. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.InputExperiments
```

### Критерій завершення блоку

- Створено `InputExperiments.java`.
- Перевірено різні методи `Scanner`.
- Досліджено щонайменше 5 варіантів вводу.
- Записано спостереження про `nextInt`, `nextDouble`, `nextLine`.
- Є ручний контрольний приклад для Mortgage Calculator.

---

## 4. Самостійне завдання — 120 хвилин

### Завдання 4.1. Створи Mortgage Calculator

Створи файл:

```text
src/main/java/daniil/dumshenko/MortgageCalculator.java
```

Програма має запитувати в користувача:

1. суму кредиту;
2. річну відсоткову ставку у відсотках;
3. строк кредиту в роках.

Після введення програма має вивести форматований щомісячний платіж.

Вимоги:

- не використовуй hardcoded значення для фінального розрахунку;
- назви змінних мають показувати одиниці виміру;
- річну ставку перетвори на місячну десяткову ставку;
- роки перетвори на кількість місячних платежів;
- для степеня використовуй `Math.pow`;
- результат форматуй через `NumberFormat.getCurrencyInstance()`;
- у фінальному коді не залишай debug-вивід без потреби.

### Завдання 4.2. Перевір некоректні значення

До перегляду готового рішення додай мінімальні перевірки:

1. principal не може бути менше або дорівнювати `0`;
2. annual interest rate не може бути від'ємною;
3. period in years не може бути менше або дорівнювати `0`;
4. дуже великий строк має бути помічений як підозрілий;
5. нульова ставка має оброблятися окремо, без ділення на нуль.

Якщо поки не знаєш зручних циклів для повторного запиту, достатньо вивести
зрозуміле повідомлення і завершити розрахунок. Не використовуй складні
конструкції, які ще не проходив, якщо можеш вирішити задачу простіше.

### Завдання 4.3. Винеси логіку в методи

Після того як базова версія працює, розділи програму на невеликі методи.

Мінімально мають бути окремі частини для:

- читання суми кредиту;
- читання річної ставки;
- читання строку;
- розрахунку щомісячного платежу;
- форматування результату;
- виводу фінального повідомлення.

Не створюй метод тільки заради методу. Кожен метод має мати одну зрозумілу
відповідальність.

### Завдання 4.4. Порівняй із рішенням курсу

Після власної реалізації переглянь рішення Mosh і запиши в журнал:

1. які назви змінних у тебе кращі або гірші;
2. чи однаково перетворені ставка та строк;
3. чи однаково використаний `Math.pow`;
4. чи є в рішенні курсу обробка нульової ставки;
5. які 2 зміни ти забираєш у свій код після порівняння;
6. які 1-2 рішення ти залишаєш своїми й чому.

### Завдання 4.5. Тестові сценарії вручну

Перевір програму з такими сценаріями:

| Principal | Annual rate | Years | Очікування |
|---:|---:|---:|---|
| `100000` | `5` | `30` | платіж приблизно близький до ручного прикладу |
| `100000` | `0` | `30` | немає ділення на нуль |
| `0` | `5` | `30` | зрозуміле повідомлення про principal |
| `-100000` | `5` | `30` | зрозуміле повідомлення про principal |
| `100000` | `-5` | `30` | зрозуміле повідомлення про ставку |
| `100000` | `5` | `0` | зрозуміле повідомлення про строк |
| `100000` | `5` | `100` | програма помічає підозріло великий строк |

У журналі для кожного сценарію запиши:

- введені значення;
- фактичний результат;
- чи результат очікуваний;
- що виправив, якщо результат був неправильний.

### Завдання 4.6. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.InputExperiments
java -cp target/classes daniil.dumshenko.MortgageCalculator
```

Потім скомпілюй Mortgage Calculator вручну:

```bash
mkdir -p /tmp/codewithmosh-day4
javac -d /tmp/codewithmosh-day4 src/main/java/daniil/dumshenko/MortgageCalculator.java
java -cp /tmp/codewithmosh-day4 daniil.dumshenko.MortgageCalculator
```

### Критерій завершення блоку

- `MortgageCalculator` приймає дані з консолі.
- Розрахунок використовує введені користувачем значення.
- Нульова ставка не спричиняє ділення на нуль.
- Від'ємні та нульові значення не дають некоректний платіж.
- Дуже великий строк помічений як підозрілий.
- Розрахунок, читання і форматування розділені на методи.
- Результат форматований як валюта.
- Програма запускається через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Перевір `MortgageCalculator`:

1. prompt-и зрозумілі користувачу;
2. principal читається з консолі;
3. annual interest rate читається як відсоток, а не готова decimal rate;
4. period читається в роках;
5. period перетворюється на кількість місяців;
6. annual rate перетворюється на monthly rate;
7. при `0%` ставка обробляється окремою формулою;
8. результат форматований як валюта;
9. немає випадкового `System.out.println` із проміжними debug-значеннями;
10. назви методів і змінних зрозумілі без коментарів.

### Завдання 5.2. Перевірка помилок

По черзі створи, прочитай і виправ три помилки:

1. прибери import для `Scanner`;
2. введи текст там, де програма очікує число;
3. тимчасово прибери окрему обробку нульової ставки.

Для кожної помилки запиши:

- що змінив або який input ввів;
- головний рядок помилки або неправильний результат;
- причину;
- як виправив;
- як уникнути такої помилки наступного разу.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Рефакторинг

Переглянь код:

- чи немає magic numbers, які треба винести в константи;
- чи не змішані читання input, розрахунок і вивід;
- чи кожен метод має одну відповідальність;
- чи зрозумілі одиниці виміру в назвах;
- чи немає дублювання формули;
- чи немає закоментованого коду;
- чи imports справді використовуються;
- чи програма поводиться передбачувано при некоректних числах.

Потім виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.MortgageCalculator
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Виконані всі функціональні перевірки.
- Досліджені та виправлені 3 помилки.
- `MortgageCalculator` запускається після чистої компіляції.
- `git diff --check` не показує whitespace errors.
- У Git видно лише очікувані зміни.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Для чого потрібен `Scanner`?
2. Що означає `System.in`?
3. Чим `nextInt()` відрізняється від `nextDouble()`?
4. Чому після `nextInt()` може виникнути проблема з `nextLine()`?
5. Що станеться, якщо користувач введе текст замість числа?
6. Які три значення потрібні для Mortgage Calculator?
7. Чому annual interest rate треба ділити на `100`?
8. Чому annual interest rate треба ділити ще й на `12`?
9. Чому period in years множиться на `12`?
10. Для чого у формулі використовується `Math.pow`?
11. Чому нульову ставку потрібно обробляти окремо?
12. Чому результат треба форматувати через `NumberFormat`?
13. Які частини програми краще винести в окремі методи?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 4. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що я змінив після порівняння з рішенням курсу
-

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і допуск:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни:

> Як Java читає дані з консолі, як введені principal, annual interest rate і
> period перетворюються для формули Mortgage Calculator, чому нульова ставка є
> особливим випадком і чому результат потрібно форматувати як валюту?

Якщо пояснення переривається, запиши прогалину до тем на повторення.

---

## Фінальний checklist Дня 4

- [ ] Закритий допуск за День 3.
- [ ] Виконане повторення без конспекту.
- [ ] Пройдені уроки Types 16-19 і Mortgage Calculator.
- [ ] Заповнена таблиця про `Scanner`, input і mortgage terms.
- [ ] Формула платежу записана й пояснена власними словами.
- [ ] Створено `InputExperiments.java`.
- [ ] Перевірено `nextInt`, `nextDouble`, `nextLine`.
- [ ] Досліджено поведінку при некоректному input.
- [ ] Підготовлено ручний контрольний приклад.
- [ ] Створено `MortgageCalculator.java`.
- [ ] Програма читає principal, annual rate і period із консолі.
- [ ] Annual rate перетворюється на monthly decimal rate.
- [ ] Period in years перетворюється на number of payments.
- [ ] Формула використовує `Math.pow`.
- [ ] Нульова ставка обробляється окремо.
- [ ] Некоректні principal, rate і period не дають некоректний платіж.
- [ ] Дуже великий строк помічений як підозрілий.
- [ ] Результат форматований через `NumberFormat`.
- [ ] Логіка розділена на методи.
- [ ] Перевірені всі ручні сценарії.
- [ ] Програма запускається через Maven.
- [ ] Програма запускається через ручний `javac`.
- [ ] Досліджені та виправлені 3 помилки.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Зміни переглянуті через Git.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `InputExperiments` і `MortgageCalculator` компілюються та запускаються.
2. `MortgageCalculator` використовує введення користувача, а не hardcoded
   значення для фінального розрахунку.
3. Ти можеш пояснити формулу платежу та одиниці виміру кожної змінної.
4. Програма не падає і не рахує неправильний платіж для нульової ставки.
5. Програма не приймає нульову або від'ємну principal amount і period.
6. Результат форматований як валюта.
7. Код розділений на зрозумілі методи без зайвого дублювання.
8. У журналі записані прогнози, помилки, порівняння з рішенням курсу,
   фактичний час і тема для повторення.
9. У Git не підготовлені випадкові сторонні зміни.

---

# День 5. Умови та логічні оператори

## Мета дня

Навчитися керувати потоком виконання програми через `if`, `else`, `else if`,
логічні оператори, ternary operator і `switch`. Закріпити тему на трьох
практичних програмах: `FizzBuzz`, `TaxCalculator` і `BmiCalculator`.

Наприкінці дня ти повинен:

- розуміти, як працюють умови `if`, `else if`, `else`;
- правильно використовувати оператори `&&`, `||`, `!`;
- пояснити short-circuit evaluation;
- знати, коли доречний ternary operator;
- знати, коли доречний `switch`;
- самостійно реалізувати FizzBuzz без перегляду рішення;
- створити `TaxCalculator` із трьома податковими діапазонами;
- створити `BmiCalculator` із категоризацією результату;
- скласти таблицю істинності для складної умови;
- перевірити крайні значення для кожної програми.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення та допуск | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення та допуск — 30 хвилин

### Завдання 1.1. Перевір готовність після Дня 4

Перед новою темою перевір, що День 4 закритий:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.InputExperiments
java -cp target/classes daniil.dumshenko.Main
git status
```

Якщо `MortgageCalculator` у твоєму проєкті запускається напряму через власний
`main`, додатково виконай:

```bash
java -cp target/classes daniil.dumshenko.MortgageCalculator
```

Якщо програма запускається через `Main`, це нормально. Важливо, щоб ти розумів,
де саме знаходиться точка входу програми.

### Завдання 1.2. Повторення без конспекту

У `learning-journal.md` додай підрозділ:

```markdown
## День 5. Повторення Дня 4
```

Без конспекту письмово відповідай:

1. Для чого потрібен `Scanner`?
2. Чому `nextInt()` може конфліктувати з `nextLine()`?
3. Що таке `InputMismatchException`?
4. Які три значення потрібні для Mortgage Calculator?
5. Чому annual interest rate треба перетворювати на monthly rate?
6. Чому `0%` ставка є особливим випадком?
7. Які частини Mortgage Calculator краще винести в окремі методи?

Після відповідей відкрий код Дня 4 і допиши короткі уточнення, якщо щось
згадав неточно.

### Завдання 1.3. Прогноз перед новою темою

Перед переглядом курсу запиши відповіді:

1. Чим `if` відрізняється від звичайного послідовного виконання коду?
2. Коли в програмі потрібен `else`?
3. Що, на твою думку, означає `&&`?
4. Що, на твою думку, означає `||`?
5. Чому умови важливі для валідації input?

### Критерій завершення блоку

- Код Дня 4 компілюється.
- Ти розумієш, через який клас запускається Mortgage Calculator.
- Є відповіді на 7 питань повторення.
- Є 5 прогнозів перед темою Control Flow.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Control Flow**:

1. Comparison Operators.
2. Logical Operators.
3. If Statements.
4. Simplifying If Statements.
5. The Ternary Operator.
6. Switch Statements.
7. Exercise: FizzBuzz.
8. Solution: FizzBuzz.

Якщо назви уроків у твоїй версії курсу трохи відрізняються, орієнтуйся на
перші уроки секції Control Flow до FizzBuzz включно.

### Як працювати з відео

1. Перед кожним уроком запиши одне питання, на яке очікуєш відповідь.
2. Після прикладу з `if` закрий відео й відтвори власний приклад.
3. Перед FizzBuzz обов'язково постав відео на паузу й спробуй задачу сам.
4. Після рішення Mosh не копіюй код механічно. Запиши, що саме відрізняється
   від твого рішення.
5. Усі приклади набирай вручну.

### Конспект

У `learning-journal.md` додай підрозділ:

```markdown
## День 5. Конспект курсу
```

Заповни таблицю:

| Поняття | Що це | Приклад | Типова помилка |
|---|---|---|---|
| `if` |  |  |  |
| `else if` |  |  |  |
| `else` |  |  |  |
| `>` / `<` |  |  |  |
| `>=` / `<=` |  |  |  |
| `==` |  |  |  |
| `!=` |  |  |  |
| `&&` |  |  |  |
| `||` |  |  |  |
| `!` |  |  |  |
| ternary operator |  |  |  |
| `switch` |  |  |  |

Окремо письмово поясни:

1. Чому для порівняння чисел використовують `==`, а не `=`?
2. Чим `&&` відрізняється від `||`?
3. Що таке short-circuit evaluation?
4. Коли ternary operator робить код кращим?
5. Коли ternary operator робить код гіршим?
6. Коли `switch` читабельніший за багато `else if`?

### Таблиця істинності

Заповни таблицю для умови:

```java
boolean canApplyDiscount = isStudent && age < 30 || hasCoupon;
```

| `isStudent` | `age < 30` | `hasCoupon` | `canApplyDiscount` |
|---|---|---|---|
| `false` | `false` | `false` |  |
| `false` | `false` | `true` |  |
| `false` | `true` | `false` |  |
| `false` | `true` | `true` |  |
| `true` | `false` | `false` |  |
| `true` | `false` | `true` |  |
| `true` | `true` | `false` |  |
| `true` | `true` | `true` |  |

Потім перепиши умову з дужками так, щоб порядок був очевидний:

```java
boolean canApplyDiscount = (isStudent && age < 30) || hasCoupon;
```

### Критерій завершення блоку

- Переглянуті уроки Control Flow 1-8.
- Таблиця понять заповнена власними словами.
- Таблиця істинності заповнена повністю.
- Є пояснення short-circuit evaluation.
- FizzBuzz спробуваний самостійно до перегляду рішення.

---

## 3. Керована практика — 60 хвилин

### Завдання 3.1. Створи клас для експериментів з умовами

Створи файл:

```text
src/main/java/daniil/dumshenko/ControlFlowExperiments.java
```

У програмі зроби невеликі експерименти:

1. перевір число на додатне, від'ємне або нуль;
2. перевір вік на категорії `child`, `teenager`, `adult`;
3. перевір, чи число парне;
4. перевір, чи число входить у діапазон від `10` до `20`;
5. перевір, чи користувач має доступ, якщо він admin або має valid token;
6. перепиши одну просту умову через ternary operator;
7. створи простий `switch` для номера дня тижня.

Для кожного експерименту запиши в `learning-journal.md`:

- які input значення перевірив;
- який результат очікував;
- який результат отримав;
- чи умова читається зрозуміло.

### Завдання 3.2. Досліди порядок умов

Створи приклад, де порядок `else if` має значення:

- `score >= 90` -> `A`;
- `score >= 75` -> `B`;
- `score >= 60` -> `C`;
- інакше -> `D`.

Потім тимчасово зміни порядок умов неправильно, наприклад постав
`score >= 60` першим. Запиши:

- який input використав;
- який результат очікував;
- який результат вийшов;
- чому порядок умов зламав логіку.

Не залишай неправильний порядок у фінальному коді.

### Завдання 3.3. Досліди short-circuit evaluation

Створи приклад, який показує, що друга частина умови може не виконатися:

```java
int denominator = 0;
boolean isSafe = denominator != 0 && 100 / denominator > 5;
```

Поясни в журналі:

1. чому програма не падає в цьому випадку;
2. що станеться, якщо замінити `&&` на `&`;
3. чому short-circuit важливий для перевірки `null` і ділення на нуль.

### Завдання 3.4. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.ControlFlowExperiments
```

### Критерій завершення блоку

- Створено `ControlFlowExperiments.java`.
- Перевірено `if`, `else if`, `else`.
- Перевірено `&&`, `||`, `!`.
- Є приклад ternary operator.
- Є приклад `switch`.
- Є приклад, де порядок `else if` впливає на результат.
- Є пояснення short-circuit evaluation.

---

## 4. Самостійне завдання — 120 хвилин

### Завдання 4.1. Реалізуй FizzBuzz без перегляду рішення

Створи файл:

```text
src/main/java/daniil/dumshenko/FizzBuzz.java
```

Програма має:

1. попросити користувача ввести ціле число;
2. якщо число ділиться на `5` і на `3`, вивести `FizzBuzz`;
3. якщо число ділиться тільки на `5`, вивести `Fizz`;
4. якщо число ділиться тільки на `3`, вивести `Buzz`;
5. інакше вивести саме число.

Перед запуском запиши прогноз для значень:

| Input | Очікуваний результат |
|---:|---|
| `15` |  |
| `10` |  |
| `9` |  |
| `7` |  |
| `0` |  |
| `-15` |  |

Важливо: спочатку зроби власне рішення, потім дивись рішення курсу.

### Завдання 4.2. Реалізуй TaxCalculator

Створи файл:

```text
src/main/java/daniil/dumshenko/TaxCalculator.java
```

Програма має читати річний дохід і рахувати податок за такими правилами:

| Дохід | Податкова ставка |
|---:|---:|
| до `10_000` включно | `0%` |
| більше `10_000` і до `40_000` включно | `10%` |
| більше `40_000` і до `100_000` включно | `20%` |
| більше `100_000` | `30%` |

Вимоги:

- негативний дохід не приймається;
- ставку винеси в зрозумілу змінну;
- податок і чистий дохід виведи окремо;
- гроші форматуй через `NumberFormat`;
- перевір граничні значення: `0`, `10000`, `10001`, `40000`, `40001`,
  `100000`, `100001`.

У журналі запиши таблицю:

| Income | Rate | Tax | Net income | Очікувано? |
|---:|---:|---:|---:|---|
| `0` |  |  |  |  |
| `10000` |  |  |  |  |
| `10001` |  |  |  |  |
| `40000` |  |  |  |  |
| `40001` |  |  |  |  |
| `100000` |  |  |  |  |
| `100001` |  |  |  |  |

### Завдання 4.3. Реалізуй BmiCalculator

Створи файл:

```text
src/main/java/daniil/dumshenko/BmiCalculator.java
```

Програма має:

1. прочитати вагу в кілограмах;
2. прочитати зріст у метрах;
3. порахувати BMI за формулою `weight / (height * height)`;
4. вивести BMI з округленням до 1-2 знаків після коми;
5. вивести категорію.

Категорії:

| BMI | Категорія |
|---:|---|
| менше `18.5` | `Underweight` |
| від `18.5` до `24.9` | `Normal` |
| від `25` до `29.9` | `Overweight` |
| `30` і більше | `Obese` |

Вимоги:

- вага має бути більше `0`;
- зріст має бути більше `0`;
- перевір граничні значення біля `18.5`, `25` і `30`;
- категоризацію зроби через `if / else if / else`;
- не використовуй `switch` для діапазонів BMI.

### Завдання 4.4. Варіанти з `if`, ternary і `switch`

У `ControlFlowExperiments.java` додай або залиш окремі приклади:

1. просту перевірку через `if`;
2. те саме рішення через ternary operator;
3. вибір тексту за фіксованим значенням через `switch`.

У журналі поясни:

- чому `switch` підходить для фіксованих значень;
- чому `switch` не підходить для діапазонів на кшталт BMI;
- чому складний ternary operator краще замінити на звичайний `if`.

### Завдання 4.5. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.ControlFlowExperiments
java -cp target/classes daniil.dumshenko.FizzBuzz
java -cp target/classes daniil.dumshenko.TaxCalculator
java -cp target/classes daniil.dumshenko.BmiCalculator
```

Потім скомпілюй самостійні класи вручну:

```bash
mkdir -p /tmp/codewithmosh-day5
javac -d /tmp/codewithmosh-day5 \
  src/main/java/daniil/dumshenko/FizzBuzz.java \
  src/main/java/daniil/dumshenko/TaxCalculator.java \
  src/main/java/daniil/dumshenko/BmiCalculator.java
java -cp /tmp/codewithmosh-day5 daniil.dumshenko.FizzBuzz
java -cp /tmp/codewithmosh-day5 daniil.dumshenko.TaxCalculator
java -cp /tmp/codewithmosh-day5 daniil.dumshenko.BmiCalculator
```

### Критерій завершення блоку

- `FizzBuzz` працює для чисел, кратних `3`, `5`, обом і жодному.
- `TaxCalculator` правильно обробляє всі межі діапазонів.
- `BmiCalculator` правильно класифікує BMI на межах категорій.
- Негативні або нульові значення не дають некоректний результат.
- Є приклади `if`, ternary і `switch`.
- Усі самостійні класи запускаються через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Перевір `FizzBuzz`:

1. `15` -> `FizzBuzz`;
2. `10` -> `Fizz`;
3. `9` -> `Buzz`;
4. `7` -> `7`;
5. `0` дає очікувану поведінку, яку ти можеш пояснити;
6. `-15` дає очікувану поведінку, яку ти можеш пояснити.

Перевір `TaxCalculator`:

1. межі `10000`, `40000`, `100000`;
2. значення одразу після меж: `10001`, `40001`, `100001`;
3. негативний input;
4. форматування валюти;
5. чи ставка не дублюється в кількох місцях без потреби.

Перевір `BmiCalculator`:

1. BMI нижче `18.5`;
2. BMI рівно або близько `18.5`;
3. BMI рівно або близько `25`;
4. BMI рівно або близько `30`;
5. нульову вагу;
6. нульовий зріст;
7. форматування BMI.

### Завдання 5.2. Перевірка помилок

По черзі створи, прочитай і виправ три помилки:

1. у FizzBuzz перевір `number % 5 == 0` перед `number % 5 == 0 && number % 3 == 0`;
2. у TaxCalculator постав умову `income > 10000` там, де треба `income <= 10000`;
3. у BmiCalculator тимчасово прибери перевірку `height > 0`.

Для кожної помилки запиши:

- що змінив;
- який input використав;
- який неправильний результат отримав;
- чому логіка зламалась;
- як виправив.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Рефакторинг

Переглянь код:

- чи умови йдуть у правильному порядку;
- чи немає дублювання однакових чисел;
- чи magic numbers варто винести в константи;
- чи назви змінних показують зміст;
- чи складні умови мають дужки для читабельності;
- чи ternary operator не став занадто складним;
- чи `switch` використаний тільки там, де він справді доречний;
- чи imports справді використовуються;
- чи всі результати у виводі підписані зрозуміло.

Потім виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.ControlFlowExperiments
java -cp target/classes daniil.dumshenko.FizzBuzz
java -cp target/classes daniil.dumshenko.TaxCalculator
java -cp target/classes daniil.dumshenko.BmiCalculator
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Виконані всі функціональні перевірки.
- Досліджені й виправлені 3 логічні помилки.
- Усі класи Дня 5 запускаються після чистої компіляції.
- `git diff --check` не показує whitespace errors.
- У Git видно лише очікувані зміни.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Для чого потрібен `if`?
2. Чим `else if` відрізняється від кількох окремих `if`?
3. Коли виконується блок `else`?
4. Чим `=` відрізняється від `==`?
5. Чим `&&` відрізняється від `||`?
6. Що робить оператор `!`?
7. Що таке short-circuit evaluation?
8. Чому порядок умов у FizzBuzz важливий?
9. Коли варто використовувати ternary operator?
10. Коли ternary operator краще не використовувати?
11. Коли `switch` читабельніший за `if / else if`?
12. Чому `switch` не підходить для BMI ranges?
13. Які крайні значення треба перевіряти в TaxCalculator?
14. Чому для BMI важливо перевірити `height > 0`?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 5. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що я змінив після порівняння з рішенням курсу
-

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і допуск:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни:

> Як у Java працюють умови, чому порядок `if / else if` важливий, як працюють
> `&&`, `||` і short-circuit evaluation, та чому FizzBuzz треба перевіряти
> спочатку на кратність і `3`, і `5` одночасно?

Якщо пояснення переривається, запиши прогалину до тем на повторення.

---

## Фінальний checklist Дня 5

- [ ] Закритий допуск за День 4.
- [ ] Виконане повторення без конспекту.
- [ ] Пройдені уроки Control Flow 1-8.
- [ ] Заповнена таблиця понять про умови й логічні оператори.
- [ ] Заповнена таблиця істинності.
- [ ] Є пояснення short-circuit evaluation.
- [ ] Створено `ControlFlowExperiments.java`.
- [ ] Перевірено `if`, `else if`, `else`.
- [ ] Перевірено `&&`, `||`, `!`.
- [ ] Є приклад ternary operator.
- [ ] Є приклад `switch`.
- [ ] Створено `FizzBuzz.java`.
- [ ] FizzBuzz реалізовано самостійно до перегляду рішення.
- [ ] Перевірені input `15`, `10`, `9`, `7`, `0`, `-15`.
- [ ] Створено `TaxCalculator.java`.
- [ ] TaxCalculator має щонайменше 4 податкові діапазони.
- [ ] Перевірені межі `10000`, `40000`, `100000`.
- [ ] Створено `BmiCalculator.java`.
- [ ] BMI класифікується через `if / else if / else`.
- [ ] Перевірені межі `18.5`, `25`, `30`.
- [ ] Нульові й від'ємні значення не дають некоректний результат.
- [ ] Гроші форматуються через `NumberFormat`, де це потрібно.
- [ ] Усі класи запускаються через Maven.
- [ ] Самостійні класи запускаються через ручний `javac`.
- [ ] Досліджені й виправлені 3 логічні помилки.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Зміни переглянуті через Git.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `ControlFlowExperiments`, `FizzBuzz`, `TaxCalculator` і `BmiCalculator`
   компілюються та запускаються.
2. Ти можеш пояснити різницю між `if`, `else if` і `else`.
3. Ти можеш пояснити різницю між `&&`, `||` і `!`.
4. Ти розумієш short-circuit evaluation і можеш навести приклад.
5. FizzBuzz працює для кратності `3`, `5`, обох і жодного.
6. TaxCalculator правильно обробляє всі межі діапазонів.
7. BmiCalculator правильно обробляє межі BMI категорій.
8. Ти розумієш, коли використовувати `switch`, а коли `if / else if`.
9. У журналі записані прогнози, помилки, фактичний час і тема для повторення.
10. У Git не підготовлені випадкові сторонні зміни.

---

# День 6. Цикли

## Мета дня

Навчитися свідомо обирати `for`, `while`, `do-while` і for-each, керувати
повтореннями через умови та правильно використовувати `break` і `continue`.
Закріпити тему на шести задачах без дублювання основної логіки.

Наприкінці дня ти повинен:

- розуміти складові циклу `for`;
- пояснювати різницю між `while` і `do-while`;
- знати, коли кількість повторень відома заздалегідь;
- уміти безпечно завершувати цикл із користувацьким input;
- використовувати `break` тільки для свідомого завершення циклу;
- використовувати `continue`, не створюючи нескінченний цикл;
- обходити масив через for-each, коли індекс не потрібний;
- реалізувати суму цифр, factorial, palindrome і prime check;
- знайти друге найбільше різне число в масиві;
- побудувати таблицю множення;
- перевірити порожні, нульові, від'ємні та граничні значення.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення та допуск | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення та допуск — 30 хвилин

### Завдання 1.1. Перевір готовність після Дня 5

З кореня проєкту виконай:

```bash
mvn test
java -cp target/classes daniil.dumshenko.ControlFlowExperiments
printf '15\n' | java -cp target/classes daniil.dumshenko.FizzBuzz
printf '40000\n' | java -cp target/classes daniil.dumshenko.TaxCalculator
printf '81\n1.8\n' | java -cp target/classes daniil.dumshenko.BmiCalculator
git diff --check
git status
```

Якщо shell не підтримує `printf` у такому вигляді, запусти класи звичайно та
введи значення вручну.

Переконайся, що:

- `15` дає `FizzBuzz`;
- дохід `40000` використовує ставку `10%`;
- вага `81` і зріст `1.8` дають BMI `25` та категорію `Overweight`;
- тести завершуються без failures;
- у Git немає випадково доданих файлів.

### Завдання 1.2. Повторення умов без конспекту

У `learning-journal.md` додай:

```markdown
## День 6. Повторення Дня 5
```

Письмово дай короткі відповіді:

1. Чому в FizzBuzz комбіновану умову потрібно перевіряти першою?
2. Чим `&&` відрізняється від `&` для boolean-виразів?
3. Чим `||` відрізняється від `|` для boolean-виразів?
4. Як short-circuit захищає від ділення на нуль?
5. Чому BMI `25` не належить до категорії `Normal`?
6. Які значення треба перевіряти по обидва боки межі `40000`?
7. Коли `switch` читабельніший за `if / else if`?

Окремо запиши результат двох виразів до запуску:

```java
int denominator = 0;
boolean first = denominator != 0 && 100 / denominator > 1;
```

```java
boolean isStudent = false;
boolean hasCoupon = true;
boolean second = isStudent && 20 < 30 || hasCoupon;
```

Поясни не тільки результат, а й які частини виразу Java фактично обчислить.

### Завдання 1.3. Прогноз перед темою циклів

Не відкриваючи конспект курсу, дай відповіді:

1. Для чого потрібен цикл?
2. Які три частини, на твою думку, має цикл `for`?
3. Коли цикл може стати нескінченним?
4. Чим `while` може відрізнятися від `do-while`?
5. Для чого можуть бути потрібні `break` і `continue`?
6. Коли для обходу масиву потрібен індекс, а коли достатньо самого значення?

### Критерій завершення блоку

- Код і тести Дня 5 працюють.
- Є відповіді на 7 питань повторення.
- Для двох boolean-виразів записаний прогноз і пояснення обчислення.
- Є 6 початкових прогнозів про цикли.
- Повторені межі та short-circuit evaluation, які були складними у День 5.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Control Flow**,
уроки 9–16:

9. For Loops.
10. While Loops.
11. Do..While Loops.
12. Break and Continue Statements.
13. For-Each Loop.
14. Project: Mortgage Calculator.
15. Solution.
16. Summary.

Перед переглядом рішення Mortgage Calculator відкрий власну реалізацію та
спочатку письмово визнач:

- які цикли вже є у твоєму коді;
- що завершує кожен цикл;
- чи може цикл стати нескінченним;
- чи дублюється логіка читання input;
- що ти хотів би покращити після курсу.

Не переписуй калькулятор механічно за відео. Порівняй підходи й запиши лише
конкретні відмінності.

### Завдання 2.1. Таблиця циклів

У `learning-journal.md` додай:

```markdown
## День 6. Конспект курсу
```

Заповни таблицю власними словами:

| Конструкція | Коли обрати | Що перевіряється | Типова помилка |
|---|---|---|---|
| `for` |  |  |  |
| `while` |  |  |  |
| `do-while` |  |  |  |
| for-each |  |  |  |
| `break` |  |  |  |
| `continue` |  |  |  |

### Завдання 2.2. Анатомія циклу `for`

Для прикладу:

```java
for (int i = 0; i < 5; i++) {
    System.out.println(i);
}
```

Письмово поясни:

1. Що виконується один раз?
2. Що перевіряється перед кожною ітерацією?
3. Коли виконується `i++`?
4. Які значення будуть виведені?
5. Скільки разів виконається тіло циклу?
6. Що зміниться, якщо написати `i <= 5`?
7. Що станеться, якщо прибрати `i++`?

### Завдання 2.3. Порівняння `while` і `do-while`

Склади таблицю:

| Питання | `while` | `do-while` |
|---|---|---|
| Коли перевіряється умова? |  |  |
| Мінімальна кількість виконань тіла |  |  |
| Типовий приклад |  |  |
| Ризик нескінченного циклу |  |  |

Окремо поясни, чому меню або запит input інколи зручно реалізувати через
`do-while`, але чому це не означає, що `do-while` завжди кращий.

### Завдання 2.4. `break`, `continue` і for-each

Письмово відповідай:

1. Чим `break` відрізняється від `return`?
2. Чим `continue` відрізняється від `break`?
3. Чому `continue` у `while` може призвести до нескінченного циклу?
4. Чому for-each зручний для читання всіх значень масиву?
5. У якому випадку for-each незручний?
6. Чи можна через for-each надійно визначити індекс поточного елемента без
   окремого лічильника?

### Завдання 2.5. Прогнози до запуску

Для кожного фрагмента спочатку запиши:

- послідовність виводу;
- кількість ітерацій;
- умову завершення;
- чи є ризик нескінченного циклу.

```java
for (int i = 3; i >= 1; i--) {
    System.out.println(i);
}
```

```java
int number = 1;
while (number < 10) {
    number *= 2;
}
System.out.println(number);
```

```java
int value = 5;
do {
    System.out.println(value);
} while (value < 5);
```

```java
for (int i = 0; i < 6; i++) {
    if (i == 2) {
        continue;
    }
    if (i == 5) {
        break;
    }
    System.out.println(i);
}
```

### Критерій завершення блоку

- Переглянуті уроки Control Flow 9–16.
- Таблиця циклів заповнена власними словами.
- Пояснена анатомія `for`.
- Порівняні `while` і `do-while`.
- Є пояснення `break`, `continue` і for-each.
- Записані прогнози для 4 фрагментів.
- Власний Mortgage Calculator порівняний із рішенням курсу.

---

## 3. Керована практика — 60 хвилин

### Завдання 3.1. Створи `LoopExperiments`

Створи файл:

```text
src/main/java/daniil/dumshenko/LoopExperiments.java
```

Додай окремі невеликі експерименти:

1. `for` виводить числа від `1` до `10`;
2. інший `for` виводить числа від `10` до `1`;
3. `while` подвоює число, доки воно менше `100`;
4. `do-while` виконує тіло хоча б один раз при початково хибній умові;
5. цикл пропускає парні числа через `continue`;
6. цикл завершується при першому числі, кратному `7`, через `break`;
7. for-each підраховує суму елементів масиву;
8. звичайний `for` виводить індекси та значення того самого масиву.

Перед кожним запуском запиши прогноз. Не об'єднуй усі приклади в один великий
цикл.

### Завдання 3.2. Одна задача трьома циклами

Порахуй суму чисел від `1` до `10`:

- через `for`;
- через `while`;
- через `do-while`.

Усі три результати мають дорівнювати `55`.

У журналі порівняй:

- де ініціалізується лічильник;
- де перевіряється умова;
- де змінюється лічильник;
- який варіант для цієї задачі найчитабельніший;
- у якому варіанті найпростіше випадково створити нескінченний цикл.

### Завдання 3.3. Безпечний цикл input

Додай експеримент зі `Scanner`:

1. програма просить вводити цілі числа;
2. після кожного числа додає його до суми;
3. input `0` завершує цикл;
4. нуль не додається до суми;
5. нечислове значення не спричиняє нескінченне повторення тієї самої помилки.

Перевір сценарії:

| Input | Очікуваний результат |
|---|---|
| `5`, `7`, `0` | сума `12` |
| `0` | сума `0` |
| `-5`, `2`, `0` | сума `-3` |
| `hello`, `4`, `0` | помилка для `hello`, потім сума `4` |

Подумай, що потрібно зробити з неправильним token у `Scanner`, перш ніж
повторити цикл.

### Завдання 3.4. Запуск

Виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.LoopExperiments
```

### Критерій завершення блоку

- Створено `LoopExperiments.java`.
- Є приклади `for`, `while`, `do-while` і for-each.
- Одна задача розв'язана трьома видами циклів.
- Перевірені `break` і `continue`.
- Реалізовано завершення input через sentinel `0`.
- Неправильний input не створює нескінченний цикл.

---

## 4. Самостійне завдання — 120 хвилин

Цей блок виконай без готових рішень. Спочатку для кожної задачі письмово
визнач input, output, межі та умову завершення циклу. Не використовуй
`StringBuilder.reverse`, streams, сортування масиву або готові бібліотечні
методи, які виконують основну частину задачі.

Створи package:

```text
src/main/java/daniil/dumshenko/day6
```

Кожна задача повинна мати окремий клас і метод із розрахунком, який не читає
input зі `Scanner`. `main` відповідає лише за демонстраційний запуск або
читання input. Це дозволить перевіряти логіку без дублювання.

### Завдання 4.1. Сума цифр

Створи:

```text
src/main/java/daniil/dumshenko/day6/DigitSum.java
```

Вимоги:

- програма приймає ціле число;
- сума рахується циклом через `% 10` і `/ 10`;
- від'ємне число обробляється передбачувано;
- для `0` результат дорівнює `0`;
- перетворювати число на `String` для підрахунку цифр не можна.

Перевір:

| Input | Очікуваний результат |
|---:|---:|
| `0` | `0` |
| `7` | `7` |
| `12345` | `15` |
| `1002` | `3` |
| `-321` | `6` |

Окремо поясни, що відбувається з останньою цифрою на кожній ітерації.

### Завдання 4.2. Factorial

Створи:

```text
src/main/java/daniil/dumshenko/day6/FactorialCalculator.java
```

Вимоги:

- factorial рахується циклом;
- `0!` дорівнює `1`;
- від'ємний input відхиляється;
- тип результату — `long`;
- програма не повинна мовчки повертати неправильне значення через overflow;
- визнач найбільший input, factorial якого поміщається в `long`.

Перевір:

| Input | Очікуваний результат |
|---:|---:|
| `0` | `1` |
| `1` | `1` |
| `5` | `120` |
| `10` | `3628800` |
| `20` | `2432902008176640000` |
| `-1` | помилка валідації |
| `21` | помилка через межу `long` |

### Завдання 4.3. Palindrome

Створи:

```text
src/main/java/daniil/dumshenko/day6/PalindromeChecker.java
```

Перевір, чи ціле невід'ємне число однаково читається зліва направо та справа
наліво.

Вимоги:

- розверни число арифметично через цикл;
- не перетворюй число на `String`;
- від'ємні числа вважай невалідним input;
- подумай про overflow під час побудови розвернутого числа.

Перевір:

| Input | Очікуваний результат |
|---:|---|
| `0` | `true` |
| `7` | `true` |
| `121` | `true` |
| `1221` | `true` |
| `123` | `false` |
| `10` | `false` |
| `-121` | помилка валідації |

### Завдання 4.4. Prime check

Створи:

```text
src/main/java/daniil/dumshenko/day6/PrimeChecker.java
```

Вимоги:

- числа менші за `2` не є prime;
- перевіряй можливі дільники циклом;
- заверши цикл одразу після знаходження дільника;
- не перевіряй дільники далі, ніж це математично потрібно;
- поясни, чому достатньо перевіряти до квадратного кореня числа.

Перевір:

| Input | Очікуваний результат |
|---:|---|
| `-5` | `false` |
| `0` | `false` |
| `1` | `false` |
| `2` | `true` |
| `3` | `true` |
| `4` | `false` |
| `17` | `true` |
| `25` | `false` |
| `97` | `true` |

### Завдання 4.5. Друге найбільше число

Створи:

```text
src/main/java/daniil/dumshenko/day6/SecondLargestFinder.java
```

Знайди друге найбільше **різне** число в масиві.

Вимоги:

- не сортуй масив;
- не змінюй початковий масив;
- пройди масив не більше одного разу;
- дублікати найбільшого значення не вважаються другим найбільшим;
- порожній масив, один елемент або відсутність другого різного значення
  повинні оброблятися явно;
- рішення має працювати з від'ємними числами.

Перевір:

| Масив | Очікуваний результат |
|---|---:|
| `{5, 1, 9, 3, 7}` | `7` |
| `{9, 9, 7}` | `7` |
| `{-5, -1, -10}` | `-5` |
| `{2, 2, 2}` | немає другого різного числа |
| `{5}` | помилка валідації |
| `{}` | помилка валідації |

### Завдання 4.6. Таблиця множення

Створи:

```text
src/main/java/daniil/dumshenko/day6/MultiplicationTable.java
```

Вимоги:

- побудуй таблицю від `1 × 1` до `10 × 10`;
- використовуй вкладені цикли;
- кожен рядок відповідає одному першому множнику;
- стовпці мають читатися рівно;
- після повної таблиці виведи окремо таблицю для введеного числа.

Перевір:

- перший результат — `1`;
- останній результат — `100`;
- у кожному рядку рівно 10 добутків;
- таблиця для `5` починається з `5 × 1 = 5` і закінчується
  `5 × 10 = 50`.

### Завдання 4.7. Ручна компіляція

Після `mvn compile` скомпілюй класи без Maven:

```bash
mkdir -p /tmp/codewithmosh-day6
javac -d /tmp/codewithmosh-day6 \
  src/main/java/daniil/dumshenko/day6/DigitSum.java \
  src/main/java/daniil/dumshenko/day6/FactorialCalculator.java \
  src/main/java/daniil/dumshenko/day6/PalindromeChecker.java \
  src/main/java/daniil/dumshenko/day6/PrimeChecker.java \
  src/main/java/daniil/dumshenko/day6/SecondLargestFinder.java \
  src/main/java/daniil/dumshenko/day6/MultiplicationTable.java
```

Запусти щонайменше по одному нормальному та одному граничному сценарію для
кожної задачі.

### Критерій завершення блоку

- Реалізовані всі 6 задач.
- Основна логіка відокремлена від `Scanner` і консольного виводу.
- Сума цифр працює для нуля й від'ємного числа.
- Factorial має явні межі для типу `long`.
- Palindrome не використовує готовий reverse.
- Prime check завершується після знаходження дільника.
- Друге найбільше число знаходиться без сортування.
- Таблиця множення побудована вкладеними циклами.
- Класи компілюються через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Повтори всі таблиці сценаріїв із блоку 4. Особливо перевір:

1. `DigitSum`: `0`, від'ємне число та число з нулями всередині.
2. `FactorialCalculator`: `0`, `20`, `21` і від'ємне число.
3. `PalindromeChecker`: парну й непарну кількість цифр, число з нулем у кінці.
4. `PrimeChecker`: значення менше `2`, найменше prime, квадрат prime-числа.
5. `SecondLargestFinder`: дублікати, всі рівні значення, лише від'ємні числа.
6. `MultiplicationTable`: кількість рядків, стовпців і останній добуток.

Якщо метод відхиляє input, повідомлення має пояснювати причину, а не лише
писати `Error`.

### Завдання 5.2. Досліди й виправ три помилки

Створи помилки по черзі, запиши результат і відразу поверни правильний код.

1. У циклі `for` заміни `i++` на `i--`, коли умова очікує зростання `i`.
   Не запускай нескінченний цикл надовго: перед запуском передбач проблему.
2. У factorial почни накопичувач із `0` замість `1` і перевір input `5`.
3. У пошуку другого найбільшого числа тимчасово дозволь дублікату максимуму
   стати другим результатом і перевір `{9, 9, 7}`.

Для кожної помилки запиши:

- змінений рядок або умову;
- input;
- прогноз;
- фактичний неправильний результат або причину, чому запуск небезпечний;
- причину помилки;
- виправлення.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Перевір цикли на типові проблеми

Переглянь кожен цикл:

- чи змінюється змінна, від якої залежить умова;
- чи немає off-by-one error;
- чи правильні початкове й кінцеве значення;
- чи потрібен тут індекс;
- чи можна замінити цикл на for-each без втрати читабельності;
- чи `break` завершує саме потрібний цикл;
- чи `continue` не пропускає оновлення лічильника;
- чи вкладений цикл справді потрібний;
- чи розрахунок не дублюється в `main`;
- чи метод має одну зрозумілу відповідальність.

### Завдання 5.4. Автоматичні тести

Додай unit tests у наявному стилі проєкту для методів із розрахунками.
Обов'язково автоматизуй щонайменше:

- 5 сценаріїв суми цифр;
- межі factorial `0`, `20`, `21`;
- 6 сценаріїв palindrome;
- 7 сценаріїв prime check;
- 5 сценаріїв другого найбільшого числа.

Тест на помилку має перевіряти, що помилка справді виникла, а не містити
порожній `assertTrue(true)`.

### Завдання 5.5. Фінальні команди

Виконай:

```bash
mvn test
java -cp target/classes daniil.dumshenko.LoopExperiments
java -cp target/classes daniil.dumshenko.day6.DigitSum
java -cp target/classes daniil.dumshenko.day6.FactorialCalculator
java -cp target/classes daniil.dumshenko.day6.PalindromeChecker
java -cp target/classes daniil.dumshenko.day6.PrimeChecker
java -cp target/classes daniil.dumshenko.day6.SecondLargestFinder
java -cp target/classes daniil.dumshenko.day6.MultiplicationTable
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Пройдені всі функціональні сценарії.
- Досліджені й виправлені 3 типові помилки циклів.
- Є автоматичні тести для основних розрахунків і меж.
- Жоден цикл не може випадково працювати нескінченно на перевірених input.
- `mvn test` завершується успішно.
- `git diff --check` не показує whitespace errors.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. З яких трьох частин складається заголовок циклу `for`?
2. У якому порядку виконуються ці частини?
3. Коли краще обрати `for`, а коли `while`?
4. Яка головна різниця між `while` і `do-while`?
5. Що робить `break`?
6. Що робить `continue`?
7. Як `continue` може спричинити нескінченний `while`?
8. Коли for-each кращий за звичайний `for`?
9. Коли for-each не підходить?
10. Що таке off-by-one error?
11. Яка умова завершує цикл у задачі суми цифр?
12. Чому `0!` дорівнює `1` у реалізації?
13. Чому factorial не можна безпечно рахувати в `long` для будь-якого input?
14. Як арифметично розвернути число?
15. Чому для prime check достатньо дільників до квадратного кореня?
16. Чому `{9, 9, 7}` має друге найбільше різне число `7`, а не `9`?
17. Чому для таблиці множення потрібні вкладені цикли?
18. Як визначити, що цикл може стати нескінченним?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 6. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Який цикл було найважче зрозуміти
-

### Що я покращив у Mortgage Calculator після курсу
-

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і допуск:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3–5 хвилин поясни:

> Як обрати між `for`, `while`, `do-while` і for-each, що завершує цикл,
> чим `break` відрізняється від `continue` та як знайти ризик нескінченного
> циклу до запуску програми?

Після цього коротко поясни алгоритм однієї самостійної задачі, не читаючи код
рядок за рядком.

---

## Фінальний checklist Дня 6

- [ ] Код і тести Дня 5 працюють.
- [ ] Повторені межі умов і short-circuit evaluation.
- [ ] Пройдені уроки Control Flow 9–16.
- [ ] Заповнена таблиця циклів.
- [ ] Пояснена анатомія `for`.
- [ ] Порівняні `while` і `do-while`.
- [ ] Пояснені `break`, `continue` і for-each.
- [ ] Власний Mortgage Calculator порівняний із рішенням курсу.
- [ ] Створено `LoopExperiments.java`.
- [ ] Одна задача реалізована через `for`, `while` і `do-while`.
- [ ] Реалізовано безпечний цикл читання input.
- [ ] Створено `DigitSum.java`.
- [ ] Створено `FactorialCalculator.java`.
- [ ] Створено `PalindromeChecker.java`.
- [ ] Створено `PrimeChecker.java`.
- [ ] Створено `SecondLargestFinder.java`.
- [ ] Створено `MultiplicationTable.java`.
- [ ] Перевірені нульові, від'ємні та граничні значення.
- [ ] Factorial захищений від overflow типу `long`.
- [ ] Друге найбільше число шукається без сортування.
- [ ] Таблиця множення побудована вкладеними циклами.
- [ ] Самостійні класи компілюються через ручний `javac`.
- [ ] Додані змістовні автоматичні тести.
- [ ] Досліджені й виправлені 3 типові помилки циклів.
- [ ] `mvn test` завершується успішно.
- [ ] `git diff --check` не показує whitespace errors.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. Ти можеш пояснити різницю між `for`, `while`, `do-while` і for-each.
2. Усі 6 самостійних задач працюють на нормальних і граничних значеннях.
3. У циклах немає відомих ризиків нескінченного виконання або off-by-one.
4. Factorial явно відхиляє значення, що не поміщаються в `long`.
5. Друге найбільше різне число знаходиться без сортування масиву.
6. Основна логіка задач не дублюється в `main`.
7. Автоматичні тести перевіряють основні та граничні сценарії.
8. `mvn test` і ручна компіляція завершуються успішно.
9. У журналі записані прогнози, труднощі, фактичний час і тема для повторення.
10. У Git не підготовлені випадкові сторонні зміни.

---

# День 7. Контрольна робота №1

## Мета дня

Без нових відео перевірити засвоєння Java Fundamentals за перший тиждень.
Головний результат — консольний `NumberAnalyzer`, створений із відокремленою
від input/output логікою та перевірений граничними сценаріями.

## Розклад дня

| Блок | Час |
|---|---:|
| Повторення й планування | 30 хв |
| Контрольна робота | 90 хв |
| Тести та негативні сценарії | 60 хв |
| Code review і рефакторинг | 90 хв |
| 20 контрольних запитань | 60 хв |
| Підсумок | 20 хв |
| **Разом** | **5 год 50 хв** |

## 1. Допуск і план

1. Запусти `mvn test` і переконайся, що День 6 завершений.
2. У журналі без коду опиши input, команду завершення, output і порожній input.
3. Запиши алгоритм обчислення count, min, max, average, even та odd.
4. Обмеж основну реалізацію 90 хвилинами; не використовуй streams.

## 2. Контрольна робота: `NumberAnalyzer`

Створи package `daniil.dumshenko.day7` і класи `NumberAnalyzer` та
`NumberStatistics`.

Вимоги:

- читати цілі числа до команди `done` або кінця input;
- не падати на нечисловому token, а пропускати його з повідомленням;
- для порожнього набору вивести `No numbers entered`;
- для непорожнього набору показати count, min, max, average, even і odd;
- підтримувати нуль, дублікати й від'ємні числа;
- обчислення винести в метод без `Scanner` і `System.out`;
- для суми використати `long`, щоб знизити ризик overflow;
- не створювати fake tests і не сортувати input без потреби.

Обов'язкові сценарії:

| Input | Очікування |
|---|---|
| `5 -2 8 8 0 done` | count 5, min -2, max 8, average 3.8, even 4, odd 1 |
| `-3 done` | усі агрегати відповідають -3, odd 1 |
| `done` | повідомлення про відсутність чисел |
| `hello 2 done` | помилка token не завершує програму, count 1 |
| EOF після `1 2` | коректне завершення й статистика |

## 3. Автоматична та ручна перевірка

Додай `Day7NumberAnalyzerTest` для змішаного набору, одного числа та
порожнього набору. Потім виконай:

```bash
mvn test
printf '5 -2 8 8 0 done\n' | java -cp target/classes daniil.dumshenko.day7.NumberAnalyzer
printf 'hello 2 done\n' | java -cp target/classes daniil.dumshenko.day7.NumberAnalyzer
printf 'done\n' | java -cp target/classes daniil.dumshenko.day7.NumberAnalyzer
```

## 4. Code review

Перевір і запиши в журналі:

- чи однаково працюють перший і наступні елементи;
- чи немає ділення на нуль для порожнього набору;
- чи average не виконує integer division;
- чи негативні числа коректно впливають на min/max і parity;
- чи нечисловий token споживається зі `Scanner`;
- чи бізнес-логіка тестується без консолі;
- чи назви пояснюють призначення змінних;
- чи кожен метод має одну відповідальність.

## 5. Контрольні запитання тижня

Письмово дай відповіді без конспекту:

1. Який шлях проходить `.java` до виконання JVM?
2. Чим primitive відрізняється від reference type?
3. Що відбувається під час narrowing cast?
4. Чому `0.1 + 0.2` неточно дорівнює `0.3`?
5. Як створити й прочитати масив?
6. Чим `Arrays.toString` відрізняється від `deepToString`?
7. Що таке operator precedence?
8. Як `Scanner` поводиться з неправильним token?
9. Навіщо валідувати input до розрахунку?
10. Коли потрібен `NumberFormat`?
11. Чим `if` відрізняється від `switch`?
12. Як працює short-circuit evaluation?
13. Чому межі діапазонів перевіряють з обох боків?
14. Коли обрати `for`?
15. Коли обрати `while`?
16. Чим `do-while` відрізняється від `while`?
17. Що роблять `break` і `continue`?
18. Коли for-each не підходить?
19. Що таке off-by-one error?
20. Як відокремити calculation від console I/O?

## 6. Підсумок

У `learning-journal.md` зафіксуй реалізовані функції, час без підказок,
помилки, результат тестів і теми на повторення.

## День вважається завершеним, якщо

1. Усі шість показників обчислюються правильно.
2. Порожній і неправильний input не спричиняє crash.
3. Логіка відокремлена від консолі й покрита тестами.
4. Пройдено щонайменше 16 із 20 контрольних запитань.
5. `mvn test` і ручні сценарії успішні.

---

# День 8. Методи та рефакторинг

## Мета дня

Застосувати Clean Code до `MortgageCalculator`: завершити Payment Schedule,
розбити відповідальності, прибрати mutable state, дублювання, magic numbers і
небезпечний input.

## Розклад дня

| Блок | Час |
|---|---:|
| Повторення Дня 7 | 30 хв |
| Clean Coding 1–10 | 90 хв |
| Аналіз до рефакторингу | 40 хв |
| Рефакторинг і Payment Schedule | 130 хв |
| Тести й порівняння | 40 хв |
| Підсумок | 20 хв |
| **Разом** | **5 год 50 хв** |

## 1. Повторення

Запусти тести Дня 7, поясни поділ між `NumberAnalyzer.main`, `analyze` і
`NumberStatistics`. Назви три ризики довгого методу та два ризики глобального
mutable state.

## 2. Матеріал курсу

Пройди **Clean Coding 1–10**. У журналі законспектуй: meaningful names,
single responsibility, extracting methods, parameters, return values,
magic numbers, duplication, side effects, comments і форматування. Для кожного
пункту наведи один приклад із поточного проєкту.

## 3. Аналіз коду до змін

Для початкового `MortgageCalculator` запиши:

- поля, які були лише проміжним станом одного розрахунку;
- неконсистентну нижню межу principal (`100` у коді проти `1000` у тексті);
- повторюваний цикл input;
- невиразну назву `readingAnnualInterestRate`;
- залежність формули від порядку виклику методів;
- відсутність безпечної обробки нечислового input;
- неможливість тестувати calculation без `Scanner`.

## 4. Рефакторинг

Виконай мінімальними кроками, запускаючи тести після кожного логічного кроку:

1. Винеси межі й коефіцієнти в `private static final` константи.
2. Прибери проміжні mutable fields із розрахунку.
3. Створи pure method `calculatePayment(principal, annualInterest, years)`.
4. Створи окремі методи читання `int` і `double` з валідацією token.
5. Додай `remainingBalance(..., paymentsMade)`.
6. Додай `paymentSchedule(...)` з одним balance для кожного місяця.
7. Переконайся, що останній balance дорівнює нулю з допустимою похибкою.
8. Не допускай principal поза `1000..1000000`, rate поза `(0..30]` і years
   поза `1..30`.

Перевір приклад `100000`, `6%`, `30 років`: payment приблизно `599.55`,
schedule має 360 елементів, balance зменшується до нуля.

## 5. Clean Code checklist і тести

Додай автоматичні тести для payment, довжини schedule, першого/останнього
balance та невалідних меж. Перевір:

- назви описують intent;
- методи короткі й мають одну відповідальність;
- calculation не читає input і не форматує output;
- немає дублювання формули або validation rules;
- коментарі не повторюють очевидний код;
- public API має передбачувані помилки;
- `main`/console orchestration не містить бізнес-формули.

Команди:

```bash
mvn test
printf '100000\n6\n30\n' | java -cp target/classes daniil.dumshenko.Main
git diff --check
```

## 6. Підсумок

У журналі порівняй код до/після, запиши рішення щодо кожного smell, результат
тестів і власний checklist із щонайменше 8 пунктів.

## День вважається завершеним, якщо

1. Розрахунки не залежать від прихованого mutable state.
2. Payment Schedule містить коректний balance для кожного місяця.
3. Неправильний console input не завершує програму exception-ом.
4. Основні формули та межі покриті тестами.
5. Методи відповідають сформованому clean-code checklist.

---

# День 9. Debugging, packaging і Maven

## Мета дня

Навчитися розрізняти compile-time, runtime і logic errors, системно читати
stack trace, користуватися debugger і створювати запускний Maven JAR.

## Розклад дня

| Блок | Час |
|---|---:|
| Повторення й baseline | 30 хв |
| Debugging and Deploying Applications | 90 хв |
| Три типи помилок | 90 хв |
| Debugger-практика | 60 хв |
| Maven package і JAR | 40 хв |
| Підсумок | 20 хв |
| **Разом** | **5 год 30 хв** |

## 1. Baseline

Виконай `mvn test`, запиши кількість тестів і переконайся, що до експериментів
код зелений. Створи `day9/DebuggingExamples.java` з методами `divide` та
`inclusiveSum`.

## 2. Три типи помилок

Створюй кожну помилку лише тимчасово й повертай правильний код:

1. Syntax/compile error: прибери `;`, виконай `mvn compile`, запиши точний
   файл, рядок, compiler message і виправлення.
2. Runtime error: виконай integer division на zero, знайди exception type,
   message, верхній frame власного коду; заміни на явну validation error.
3. Logic error: у сумі `0..limit` використай `< limit`, перевір limit `5`,
   очікувані `15` проти фактичних `10`; виправ off-by-one.

## 3. Debugger-практика в IntelliJ IDEA

Постав breakpoint у циклі `inclusiveSum` і виконай:

- Resume до breakpoint;
- Step Over для оновлення sum;
- Step Into для виклику `divide`;
- Step Out назад до caller;
- додай watches для `number`, `limit`, `sum`;
- використай conditional breakpoint `number == limit`;
- відкрий Frames і визнач caller/callee.

У журналі запиши спостереження. Цей блок потребує ручного виконання в IDE і не
вважається перевіреним лише запуском CLI.

## 4. Інструкція діагностики

Сформуй алгоритм:

1. Відтворити помилку найменшим стабільним input.
2. Прочитати тип і message, для compile error — першу релевантну діагностику.
3. У stack trace знайти перший frame власного package.
4. Сформувати прогноз про неправильне значення або branch.
5. Поставити breakpoint до місця помилки, використати watches і кроки.
6. Виправити причину мінімальним diff.
7. Додати regression test і запустити весь релевантний набір.

## 5. Maven packaging

Налаштуй `maven-jar-plugin` із `Main-Class`, який не очікує інтерактивного
input. Виконай:

```bash
mvn clean package
jar tf target/CodeWithMosh-1.0-SNAPSHOT.jar
unzip -p target/CodeWithMosh-1.0-SNAPSHOT.jar META-INF/MANIFEST.MF
java -jar target/CodeWithMosh-1.0-SNAPSHOT.jar
```

Запиши різницю між `compile`, `test` і `package`, призначення `target/classes`,
`target/test-classes`, JAR та `META-INF/MANIFEST.MF`.

## 6. Підсумок

У журналі зафіксуй три помилки, debugging checklist, точні команди packaging,
результат JAR і ручні кроки, які ще треба виконати в IntelliJ.

## День вважається завершеним, якщо

1. Три класи помилок відтворені, пояснені та виправлені.
2. Logic і runtime errors мають regression tests.
3. Stack trace читається від exception до першого власного frame.
4. Debugger-кроки виконані вручну в IDE.
5. `mvn package` і `java -jar` завершуються успішно.

---

# День 10. Classes, objects і memory allocation

## Мета дня

Перейти від procedural коду до базового object-oriented programming:
навчитися створювати класи й об'єкти, відрізняти їхній стан від поведінки,
використовувати constructors і розуміти, як references пов'язують змінні з
objects. Закріпити тему на першій доменній моделі `Personal Finance CLI`.

Наприкінці дня ти повинен:

- пояснити різницю між class, object та reference;
- розрізняти instance fields, local variables і parameters;
- створювати object через constructor і пояснювати роль `new`;
- розуміти, що два objects одного class мають незалежний state;
- розуміти, що дві references можуть вказувати на один object;
- намалювати спрощену схему stack і heap для заданого фрагмента коду;
- пояснити, коли object стає недосяжним і кандидатом для garbage collection;
- визначати responsibilities та invariants класу до написання коду;
- створити й перевірити класи `Money`, `Category` і `Transaction`;
- не змішувати domain logic із console input/output;
- порівняти procedural та object-oriented представлення одних даних;
- перевірити constructors, незалежність objects і текстове представлення.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення та допуск | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення та допуск — 30 хвилин

### Завдання 1.1. Перевір готовність після Дня 9

Перед новою темою переконайся, що baseline проєкту зелений:

```bash
mvn test
mvn package
java -jar target/CodeWithMosh-1.0-SNAPSHOT.jar
git status
```

Запиши в `learning-journal.md`:

- кількість виконаних тестів;
- чи завершився build успішно;
- що вивів запускний JAR;
- які зміни показує Git до початку Дня 10.

Якщо якась команда падає, не переходь одразу до нового коду. Спочатку визнач:

1. це compile-time, runtime чи logic error;
2. перший релевантний рядок повідомлення;
3. чи пов'язана проблема з кодом Дня 9;
4. який найменший крок відновлює зелений baseline.

### Завдання 1.2. Повторення без конспекту

У `learning-journal.md` додай підрозділ:

```markdown
## День 10. Повторення Дня 9
```

Без конспекту письмово відповідай:

1. Чим compile-time error відрізняється від runtime error?
2. Чому logic error може не спричинити exception?
3. З якого місця починають читати stack trace?
4. Що таке stack frame у контексті виклику method?
5. Для чого потрібні breakpoint, Step Over, Step Into і Step Out?
6. Чим `mvn compile`, `mvn test` і `mvn package` відрізняються?
7. Для чого JAR містить `META-INF/MANIFEST.MF`?
8. Як regression test захищає вже виправлену помилку?

Потім відкрий записи Дня 9 і коротко виправ або доповни неточні відповіді.

### Завдання 1.3. Прогноз перед новою темою

До перегляду курсу запиши власні припущення:

1. Чим class може відрізнятися від object?
2. Які дані object повинен зберігати всередині себе?
3. Для чого constructor, якщо fields можна було б заповнити пізніше?
4. Що, на твою думку, повертає вираз із `new`?
5. Чи зміниться другий object, якщо змінити перший object того самого class?
6. Що станеться, якщо дві variables посилаються на той самий object?
7. Де приблизно можуть зберігатися local variable та створений object?

Не шукай правильні відповіді перед прогнозом. Мета — порівняти початкову
модель із тим, що ти зрозумієш після практики.

### Критерій завершення блоку

- `mvn test` і `mvn package` успішні до змін Дня 10.
- Запускний JAR із Дня 9 працює.
- Зафіксований початковий Git-стан.
- Є відповіді на 8 питань повторення.
- Є 7 прогнозів про classes, objects, references і memory.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди перші уроки секції **Ultimate Java Part 2: Object-oriented
Programming -> Classes** до теми memory allocation включно.

Орієнтовний обсяг:

1. Classes.
2. Creating Classes.
3. Creating Objects.
4. Memory Allocation.
5. Procedural Programming.
6. Encapsulation або наступний вступний урок, якщо він входить у перші шість
   уроків твоєї версії курсу.

Назви та порядок уроків можуть трохи відрізнятися. Не переходь цього дня до
inheritance, polymorphism або interfaces: спочатку закріпи базову модель
class/object/reference.

### Як працювати з відео

1. Перед кожним уроком запиши одне питання, на яке очікуєш відповідь.
2. Після прикладу class постав відео на паузу й назви його state та behavior.
3. Після створення object відтвори приклад вручну, не копіюючи код.
4. Перед поясненням memory allocation намалюй власний прогноз stack/heap.
5. Після пояснення виправ схему іншим кольором або окремим списком.
6. Після кожних двох уроків закрий конспект і запиши 3-5 тез із пам'яті.
7. Приклад procedural programming спочатку оціни сам: які дані можуть бути
   випадково роз'єднані або переплутані?

### Завдання 2.1. Конспект понять

У `learning-journal.md` додай підрозділ:

```markdown
## День 10. Конспект курсу
```

Заповни таблицю власними словами:

| Поняття | Що це | Приклад без готового коду | Типова помилка |
|---|---|---|---|
| class |  |  |  |
| object / instance |  |  |  |
| field |  |  |  |
| method |  |  |  |
| constructor |  |  |  |
| parameter |  |  |  |
| local variable |  |  |  |
| reference |  |  |  |
| state |  |  |  |
| behavior |  |  |  |
| `new` |  |  |  |
| stack frame |  |  |  |
| heap |  |  |  |
| garbage collection |  |  |  |

### Завдання 2.2. Class, object і reference

Письмово поясни:

1. Чому class називають шаблоном, але class не є готовим object?
2. Чи можуть два objects одного class мати різний state?
3. Чи може одна reference в різний час посилатися на різні objects?
4. Чи може object існувати без доступної reference у програмі?
5. Чим `null` відрізняється від object із порожніми fields?
6. Чому порівнювати references — не завжди те саме, що порівнювати вміст?

Для кожної відповіді наведи короткий приклад словами, а не копіюй визначення.

### Завдання 2.3. State, behavior і responsibilities

Для трьох предметів — банківського рахунку, автомобіля та книги — склади
таблицю:

| Предмет | Можливий state | Можлива behavior | Що не належить цьому class |
|---|---|---|---|
| Банківський рахунок |  |  |  |
| Автомобіль |  |  |  |
| Книга |  |  |  |

Після таблиці відповідай:

1. Чому не кожне пов'язане з предметом значення має бути його field?
2. Як визначити, що method належить конкретному class?
3. Що станеться з class, який має забагато різних responsibilities?

### Завдання 2.4. Спрощена модель stack і heap

Намалюй від руки або в журналі три схеми:

1. local primitive variable без створення object;
2. одна local reference та один object;
3. дві local references на один object.

Для кожної схеми познач:

- активний method і його stack frame;
- local variables;
- reference окремо від object;
- fields усередині object;
- напрямок зв'язку reference -> object;
- момент, коли object перестає бути reachable.

Важливо: це навчальна спрощена модель JVM. Не стверджуй, що конкретна JVM
завжди фізично розміщує кожне значення саме так через можливі оптимізації.

### Критерій завершення блоку

- Пройдені перші уроки секції Classes до memory allocation включно.
- Таблиця з 14 поняттями заповнена власними словами.
- Є відповіді про class, object, reference і `null`.
- State та behavior розділені для трьох прикладів.
- Намальовані три схеми stack/heap.
- Початковий прогноз порівняний із поясненням курсу.

---

## 3. Керована практика — 60 хвилин

### Завдання 3.1. Перевір наявний `Money`

Відкрий файл:

```text
src/main/java/daniil/dumshenko/day10/Money.java
```

Не переписуй його одразу. Спочатку в журналі запиши:

- які fields уже є;
- які constructors і methods уже є;
- що є state, а що behavior;
- які значення зараз може мати object;
- які некоректні значення class поки дозволяє створити;
- чи можна змінити state після створення і чи це потрібно;
- чи має class зайві responsibilities.

Якщо файл порожній або містить лише початковий експеримент, зафіксуй це й
використай його як основу. Не видаляй роботу без аналізу.

### Завдання 3.2. Два незалежні objects

Створи навчальний клас запуску:

```text
src/main/java/daniil/dumshenko/day10/ObjectExperiments.java
```

У `main` створи два різні objects `Money` з різними або однаковими початковими
значеннями. Перед запуском заповни таблицю:

| Крок | Прогноз для першого object | Прогноз для другого object |
|---|---|---|
| Після створення |  |  |
| Після зміни лише першого |  |  |
| Після повторного виводу |  |  |

Якщо твій фінальний `Money` буде immutable, цей експеримент можна виконати до
фінального рефакторингу або створити новий object замість зміни fields. У
журналі чітко розділи mutation object і переприсвоєння reference.

Після запуску поясни:

1. чому objects мають незалежний state;
2. чому однакові значення fields не роблять їх одним object;
3. скільки викликів `new` і скільки objects було створено.

### Завдання 3.3. Дві references на один object

Створи другу reference на перший object без другого виклику `new`. Перевір
state через обидві references до та після зміни.

Перед запуском запиши прогноз:

| Перевірка | Очікуваний результат | Чому |
|---|---|---|
| State через першу reference |  |  |
| State через другу reference |  |  |
| Кількість створених objects |  |  |
| Результат після mutation |  |  |

Після запуску поясни різницю між:

- створенням нового object;
- копіюванням reference;
- переприсвоєнням reference;
- зміною state object.

### Завдання 3.4. Reachability і garbage collection

На папері або в журналі продовж схему попереднього завдання для трьох станів:

1. дві references вказують на один object;
2. одна reference отримує `null` або починає вказувати на інший object;
3. жодна reachable reference більше не вказує на початковий object.

Для кожного стану вкажи:

- скільки reachable references має object;
- чи можна ще звернутися до object із поточного коду;
- чи став він кандидатом для garbage collection;
- чому кандидат не означає, що пам'ять буде звільнена негайно.

Не використовуй `System.gc()` як доказ моментального видалення object.

### Завдання 3.5. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.day10.ObjectExperiments
```

Порівняй фактичний результат із кожним прогнозом. Несподіваний результат не
стирай із журналу — поясни причину поруч.

### Критерій завершення блоку

- Проаналізовано наявний `Money.java` до змін.
- Створено `ObjectExperiments.java`.
- Перевірено два незалежні objects.
- Перевірено дві references на один object.
- Пояснено різницю між mutation і reference reassignment.
- Намальовано три стани reachability.
- Пояснено, чому garbage collection не запускається за вимогою програми.
- Фактичні результати порівняні з прогнозами.

---

## 4. Самостійне завдання — 120 хвилин

### Загальний контекст

Почни доменну модель майбутнього `Personal Finance CLI`. Цього дня потрібні
лише objects у пам'яті. Не додавай `Scanner`, menu, repository, service,
database, файли, inheritance або framework.

Усі класи Дня 10 розміщуй у package:

```text
daniil.dumshenko.day10
```

Перед написанням коду створи в журналі таблицю дизайну:

| Class | Responsibility | State | Behavior | Invariants |
|---|---|---|---|---|
| `Money` |  |  |  |  |
| `Category` |  |  |  |  |
| `Transaction` |  |  |  |  |

### Завдання 4.1. Заверши `Money`

Використай наявний файл:

```text
src/main/java/daniil/dumshenko/day10/Money.java
```

Class має представляти грошову суму та currency.

Мінімальні вимоги:

- amount зберігається як числове значення;
- currency зберігається як непорожній текст;
- constructor отримує всі обов'язкові дані;
- object не можна створити з `NaN` або positive/negative infinity;
- `null`, порожня або blank currency відхиляються;
- class надає контрольований доступ до state;
- вивід показує зміст object, а не default `ClassName@hash`;
- domain class не читає input і не друкує результат самостійно.

До коду письмово виріши:

1. Чи дозволяє твоя модель negative amount?
2. Якщо дозволяє, що означає знак у `Money`?
3. Якщо не дозволяє, який class надалі відповідатиме за income/expense?
4. Чи повинен `Money` змінюватися після створення?
5. Які methods справді потрібні сьогодні, а які були б передчасними?

Не використовуй `double` для production-фінансів без пояснення його обмежень.
Для поточного навчального етапу можеш залишити тип, який уже є в проєкті, але
зафіксуй ризик rounding і майбутню альтернативу на кшталт `BigDecimal`.

### Завдання 4.2. Створи `Category`

Створи файл:

```text
src/main/java/daniil/dumshenko/day10/Category.java
```

Class має описувати категорію transaction.

Вимоги:

- category має name;
- category має type `INCOME` або `EXPENSE`;
- не використовуй довільні String-значення для type, якщо можеш обмежити
  набір допустимих значень мовою Java;
- `null`, порожній або blank name відхиляються;
- відсутній type відхиляється;
- constructor створює одразу валідний object;
- текстове представлення містить name і type;
- class не відповідає за читання menu або зберігання списку transactions.

Перед реалізацією поясни, чому fixed set із двох значень безпечніший за
довільний текст і яку помилку він не дозволяє зробити.

### Завдання 4.3. Створи `Transaction`

Створи файл:

```text
src/main/java/daniil/dumshenko/day10/Transaction.java
```

Class має об'єднати:

- description;
- amount як object `Money`;
- category як object `Category`;
- date.

Вимоги:

- усі чотири значення передаються через constructor;
- `null` для amount, category або date відхиляється;
- `null`, порожній або blank description відхиляється;
- `Transaction` використовує вже створені domain objects, а не дублює їхні
  fields окремими primitive/String parameters;
- текстове представлення дозволяє побачити всі важливі дані transaction;
- class не читає консоль, не друкує себе й не зберігає глобальний список;
- не додавай setters автоматично: кожну допустиму mutation спочатку
  обґрунтуй правилом предметної області.

Окремо письмово визнач семантику amount:

- або amount завжди non-negative, а напрям задає `Category` type;
- або знак amount кодує напрям і має бути узгоджений із `Category` type.

Обери один варіант і застосуй його послідовно. Не залишай ситуацію, де
negative expense випадково перетворюється на income без чіткого правила.

### Завдання 4.4. Створи сценарій `PersonalFinanceDemo`

Створи файл:

```text
src/main/java/daniil/dumshenko/day10/PersonalFinanceDemo.java
```

У `main`:

1. створи щонайменше одну income category;
2. створи щонайменше дві expense categories;
3. створи щонайменше чотири transactions;
4. повторно використай одну category у двох transactions;
5. виведи кожну transaction зрозумілим текстом;
6. продемонструй, що пов'язані дані передаються як objects;
7. не додавай інтерактивний input — використовуй контрольовані test data.

Рекомендований набір сценаріїв для ручної перевірки:

| Description | Category type | Amount | Очікування |
|---|---|---:|---|
| Salary | `INCOME` | додатне | Object створено |
| Groceries | `EXPENSE` | додатне | Object створено |
| Transport | `EXPENSE` | додатне | Object створено |
| Друга покупка | `EXPENSE` | додатне | Та сама category повторно використана |

Назви та суми обери самостійно. Таблиця описує поведінку, а не готовий код.

### Завдання 4.5. Procedural проти OOP

У `PersonalFinanceDemo` або окремому короткому експерименті представ одну
transaction двома способами:

1. procedural — окремими local variables;
2. object-oriented — через `Money`, `Category` і `Transaction`.

У журналі порівняй:

| Критерій | Окремі variables | Domain objects |
|---|---|---|
| Де зібрані пов'язані дані |  |  |
| Хто перевіряє invariants |  |  |
| Ризик переплутати arguments |  |  |
| Додавання другої transaction |  |  |
| Повторне використання category |  |  |
| Тестованість |  |  |
| Кількість початкового коду |  |  |

Зроби збалансований висновок: domain objects корисні для моделі, що росте,
але кілька local variables можуть бути простішими для одноразового маленького
розрахунку. Не пиши, що OOP автоматично завжди краще.

### Завдання 4.6. Перевір неправильні значення

Тимчасово спробуй створити objects із такими даними:

| Class | Некоректні дані | Очікувана поведінка |
|---|---|---|
| `Money` | `null` currency | Створення відхилено |
| `Money` | blank currency | Створення відхилено |
| `Money` | `NaN` amount | Створення відхилено |
| `Money` | infinite amount | Створення відхилено |
| `Category` | blank name | Створення відхилено |
| `Category` | `null` type | Створення відхилено |
| `Transaction` | blank description | Створення відхилено |
| `Transaction` | `null` amount | Створення відхилено |
| `Transaction` | `null` category | Створення відхилено |
| `Transaction` | `null` date | Створення відхилено |

Для кожного випадку запиши actual exception type та message. Не залишай
навмисно неправильне створення objects у фінальному `main`.

### Завдання 4.7. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.day10.ObjectExperiments
java -cp target/classes daniil.dumshenko.day10.PersonalFinanceDemo
```

Потім вручну скомпілюй package Дня 10 в окрему директорію. У PowerShell:

```powershell
New-Item -ItemType Directory -Force target/day10-manual
$day10Sources = Get-ChildItem src/main/java/daniil/dumshenko/day10/*.java | ForEach-Object { $_.FullName }
javac -d target/day10-manual $day10Sources
java -cp target/day10-manual daniil.dumshenko.day10.ObjectExperiments
java -cp target/day10-manual daniil.dumshenko.day10.PersonalFinanceDemo
```

### Критерій завершення блоку

- Перед кодом визначені responsibilities та invariants трьох classes.
- `Money` має валідні amount і currency.
- `Category` обмежує type значеннями `INCOME` та `EXPENSE`.
- `Transaction` використовує `Money` і `Category` як objects.
- Визначена послідовна семантика amount.
- Domain classes не залежать від console I/O та global mutable state.
- Створені щонайменше 3 categories і 4 transactions.
- Default `ClassName@hash` не потрапляє у фінальний output.
- Перевірені всі invalid cases із таблиці.
- Є збалансоване порівняння procedural та OOP.
- Класи запускаються через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Додай автоматичні тести

Створи tests у package:

```text
src/test/java/daniil/dumshenko/day10
```

Дотримуйся JUnit-стилю, який уже використовується в проєкті. Додай реальні
перевірки для таких сценаріїв:

`Money`:

1. valid amount і currency зберігаються правильно;
2. `null` currency відхиляється;
3. empty і blank currency відхиляються;
4. `NaN` відхиляється;
5. positive та negative infinity відхиляються;
6. textual representation містить amount і currency.

`Category`:

1. valid name і type зберігаються правильно;
2. `null`, empty і blank name відхиляються;
3. `null` type відхиляється;
4. textual representation містить name і type.

`Transaction`:

1. valid transaction зберігає передані objects і date;
2. invalid description відхиляється;
3. `null` amount, category і date відхиляються окремими tests;
4. правило узгодженості amount/category виконується, якщо воно є в моделі;
5. textual representation містить ключові дані.

Objects і references:

1. два окремо створені objects не ділять mutable state;
2. дві references на один mutable object бачать той самий state — лише якщо
   фінальний design справді допускає mutation;
3. immutable design перевіряється через відсутність небезпечної зміни state,
   а не через fake-test.

### Завдання 5.2. Перевір межі відповідальності

Переглянь кожен domain class і відповідай `так` або `ні`:

- Чи class має одну зрозумілу responsibility?
- Чи всі його fields належать одному domain concept?
- Чи constructor гарантує invariants?
- Чи object не залишається частково ініціалізованим?
- Чи немає `public` mutable fields?
- Чи setters не додані лише «про всяк випадок»?
- Чи немає `Scanner`, `System.in` або `System.out` у domain class?
- Чи немає `static` mutable collection із transactions?
- Чи назви methods описують behavior, а не деталі реалізації?
- Чи textual representation корисне для людини?

Якщо відповідь `ні`, зроби мінімальний рефакторинг і запиши причину.

### Завдання 5.3. Досліди й виправ три помилки

По черзі створи лише тимчасово, відтвори й виправ:

1. скопіюй reference замість створення другого object і помилково очікуй
   незалежний state;
2. дозволь blank currency або category name;
3. виведи object без корисного textual representation та отримай
   `ClassName@hash`.

Для кожної помилки запиши:

- що змінив;
- який результат прогнозував;
- який результат отримав;
- яку неправильну модель class/object/reference виявив;
- як виправив;
- який test захищає виправлення.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.4. Фінальні команди

Виконай:

```bash
mvn test
mvn package
java -cp target/classes daniil.dumshenko.day10.ObjectExperiments
java -cp target/classes daniil.dumshenko.day10.PersonalFinanceDemo
java -jar target/CodeWithMosh-1.0-SNAPSHOT.jar
git diff --check
git status
git diff --stat
```

Якщо `Main-Class` із Дня 9 ще не запускає demo Дня 10, це не помилка: JAR має
залишатися працездатним, а `PersonalFinanceDemo` запускається окремою командою.
Не змінюй entry point лише заради дублювання одного запуску.

### Критерій завершення блоку

- Додані tests для valid та invalid domain objects.
- Tests справді перевіряють behavior, а не лише факт запуску.
- Перевірені межі responsibilities трьох classes.
- Досліджені й виправлені 3 типові OOP-помилки.
- `mvn test` і `mvn package` успішні.
- Обидва класи запуску працюють.
- `git diff --check` не показує whitespace errors.
- Git містить лише очікувані зміни.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Чим class відрізняється від object?
2. Чим object відрізняється від reference?
3. Що є state, а що behavior?
4. Для чого потрібен constructor?
5. Що приблизно відбувається під час виконання `new`?
6. Чим instance field відрізняється від local variable і parameter?
7. Чому два objects одного class мають незалежний state?
8. Чому дві references можуть бачити одну й ту саму зміну state?
9. Чим створення object відрізняється від копіювання reference?
10. Що означає `null` reference?
11. Що приблизно показують stack і heap у спрощеній моделі?
12. Коли object стає unreachable?
13. Чому garbage collector не зобов'язаний одразу звільнити пам'ять?
14. Що таке responsibility class?
15. Що таке invariant і хто має його захищати?
16. Чому `public` mutable fields небезпечні?
17. Чому domain class не повинен читати `Scanner`?
18. Чому дві String-параметри легше переплутати, ніж два domain objects?
19. Які invariants мають `Money`, `Category` і `Transaction`?
20. Коли procedural рішення може бути простішим і достатнім?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 10. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Class, object і reference власними словами
- Class:
- Object:
- Reference:

### Моя схема stack і heap
- Посилання на схему або текстовий опис:

### Responsibilities та invariants
- Money:
- Category:
- Transaction:

### Procedural проти OOP
- Перевага domain objects:
- Trade-off domain objects:
- Коли procedural варіант достатній:

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:
  Regression test:

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і допуск:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 5 хвилин поясни:

> Чим class відрізняється від object і reference, як два objects одного class
> зберігають незалежний state, чому дві references можуть вказувати на один
> object, що показує спрощена модель stack/heap і як constructors захищають
> invariants доменної моделі?

Потім окремо за 1-2 хвилини поясни, чому `Money`, `Category` і `Transaction`
кращі за набір непов'язаних variables у моделі, що буде розширюватися.

Якщо пояснення переривається, запиши конкретну прогалину до тем повторення.

---

## Фінальний checklist Дня 10

- [ ] Закритий допуск за День 9.
- [ ] `mvn test`, `mvn package` і запуск JAR успішні до нових змін.
- [ ] Виконане повторення без конспекту.
- [ ] Записані прогнози про class, object, reference і memory.
- [ ] Пройдені уроки Classes до memory allocation включно.
- [ ] Заповнена таблиця з 14 ключовими поняттями.
- [ ] Пояснена різниця між class, object і reference.
- [ ] Пояснена різниця між field, local variable і parameter.
- [ ] Намальовані три схеми stack/heap.
- [ ] Позначено момент, коли object стає unreachable.
- [ ] Проаналізовано початковий стан `Money.java`.
- [ ] Створено `ObjectExperiments.java`.
- [ ] Перевірено два незалежні objects.
- [ ] Перевірено дві references на один object.
- [ ] Пояснена різниця між mutation та reference reassignment.
- [ ] Визначені responsibilities та invariants domain classes.
- [ ] Завершено `Money.java`.
- [ ] `Money` відхиляє invalid amount і currency.
- [ ] Створено `Category.java`.
- [ ] Category type обмежений значеннями `INCOME` та `EXPENSE`.
- [ ] Створено `Transaction.java`.
- [ ] `Transaction` використовує `Money` та `Category` як objects.
- [ ] Визначена семантика amount для income й expense.
- [ ] Domain classes не залежать від console I/O.
- [ ] Немає `public` mutable fields або global mutable state.
- [ ] Створено `PersonalFinanceDemo.java`.
- [ ] Створені щонайменше 3 categories і 4 transactions.
- [ ] Одна category повторно використана кількома transactions.
- [ ] Textual representation не містить default `ClassName@hash`.
- [ ] Перевірені invalid constructor arguments.
- [ ] Додані автоматичні tests Дня 10.
- [ ] Перевірена незалежність objects або immutable design.
- [ ] Досліджені й виправлені 3 типові помилки.
- [ ] Зроблено порівняння procedural та OOP із trade-offs.
- [ ] Класи запускаються через Maven.
- [ ] Package Дня 10 компілюється ручним `javac`.
- [ ] `mvn test` і `mvn package` успішні після змін.
- [ ] `git diff --check` не показує whitespace errors.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. Ти можеш без конспекту пояснити class, object, reference, state, behavior і
   constructor.
2. Ти можеш намалювати дві незалежні objects і дві references на один object
   у спрощеній моделі stack/heap.
3. Ти розумієш різницю між mutation object і переприсвоєнням reference.
4. `Money`, `Category` і `Transaction` мають чіткі responsibilities та
   захищають визначені invariants.
5. Domain classes не містять console I/O, `public` mutable fields або global
   mutable state.
6. `PersonalFinanceDemo` створює і показує щонайменше 4 valid transactions.
7. Invalid constructor arguments перевірені автоматичними tests.
8. Procedural та OOP варіанти порівняні без твердження, що один підхід завжди
   кращий.
9. `mvn test`, `mvn package`, обидва demo-запуски та ручний `javac` успішні.
10. У журналі записані прогнози, схеми, помилки, результати tests, фактичний
    час і тема для повторення.
11. У Git видно лише очікувані зміни без whitespace errors.
