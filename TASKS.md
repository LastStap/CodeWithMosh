# Щоденні завдання Java Backend Developer

Цей файл деталізує програму з `CourseProgramm.md`.

## Правила ведення файлу

- За один раз до файлу додаються завдання лише для **одного навчального дня**.
- Наступний день додається тільки після окремого запиту.
- Нові завдання мають спиратися на відповідний день із `CourseProgramm.md`.
- Перед додаванням наступного дня потрібно врахувати результат попереднього:
  виконані завдання, труднощі, помилки та теми для повторення.
- Не потрібно переходити до наступного дня, якщо не виконані критерії
  завершення поточного дня.
- Орієнтовна тривалість дня — **5 годин 30 хвилин чистого навчального часу**.
- Після кожних 50-60 хвилин роботи потрібно зробити перерву 10-15 хвилин.

---

# День 1. Середовище, перша Java-програма та Git

## Мета дня

Підготувати й перевірити робоче середовище, зрозуміти базову структуру
Java-програми, навчитися запускати її з IntelliJ IDEA та термінала, а також
перевірити базове розуміння Git.

Наприкінці дня ти повинен:

- знати встановлені версії Java, Maven і Git;
- розуміти призначення JDK, JVM, JRE, source code та bytecode;
- самостійно написати клас `HelloDeveloper`;
- скомпілювати й запустити програму з IntelliJ IDEA;
- скомпілювати й запустити програму з термінала;
- знайти створений `.class`-файл;
- розуміти базову структуру наявного Maven-проєкту;
- переглянути зміни через Git і підготувати змістовний commit.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Стартова діагностика | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Стартова діагностика — 30 хвилин

У перший день немає матеріалу за вчора, тому замість повторення потрібно
зафіксувати початковий рівень. Не користуйся пошуком, конспектом або AI під
час відповідей.

### Завдання 1.1. Дай письмові відповіді

Створи файл `learning-journal.md`, якщо його ще немає, і додай заголовок:

```markdown
# Learning Journal

## День 1. Стартова діагностика
```

Своїми словами дай короткі відповіді на запитання:

1. Що таке Java?
2. Чим Java відрізняється від JavaScript?
3. Що робить компілятор?
4. Що таке JVM?
5. Для чого потрібен метод `main`?
6. Чим вихідний файл `.java` відрізняється від `.class`?
7. Що таке Maven?
8. Що таке Git repository?
9. Чим `git add` відрізняється від `git commit`?
10. Як ти зазвичай шукаєш причину помилки в програмі?

Не виправляй відповіді одразу. Наприкінці дня повернися до них і доповни
іншим кольором у редакторі або окремим підрозділом `Уточнення після навчання`.

### Завдання 1.2. Самооцінка

Постав оцінку від 0 до 5:

- Java syntax;
- робота з IntelliJ IDEA;
- робота з терміналом;
- Maven;
- Git;
- читання повідомлень про помилки;
- технічна англійська.

Поруч із кожною оцінкою напиши одне речення, чому поставлена саме вона.

### Критерій завершення блоку

- Є відповіді на всі 10 запитань.
- Є 7 оцінок із коротким поясненням.
- Відповіді написані з пам'яті.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Getting Started**:

1. Setting Up the Development Environment.
2. Anatomy of a Java Program.
3. Your First Java Program.
4. How Java Code Gets Executed.
5. 5 Interesting Facts about Java.
6. Course Structure.

Пункти `Source Code`, `Follow Me Around` і `Learning Paths` достатньо
переглянути без детального конспектування.

### Як працювати з відео

1. Перед кожним уроком прочитай його назву та сформулюй одне запитання, на яке
   очікуєш отримати відповідь.
2. Під час першого перегляду не копіюй код механічно.
3. Після кожних 2-3 відео постав відтворення на паузу.
4. Закрий відео й запиши 3-5 ключових тез із пам'яті.
5. Повернися до відео та виправ лише фактичні помилки у своїх нотатках.
6. Усі приклади коду набирай вручну.

### Конспект

У `learning-journal.md` додай підрозділ:

```markdown
## День 1. Конспект курсу
```

Заповни таблицю власними словами:

| Поняття | Що це | Для чого потрібно |
|---|---|---|
| Source code |  |  |
| Compiler |  |  |
| Bytecode |  |  |
| JDK |  |  |
| JVM |  |  |
| JRE |  |  |
| `class` |  |  |
| `main` |  |  |
| `System.out.println` |  |  |

### Схема виконання Java-коду

Намалюй у конспекті та поясни кожен перехід:

```text
HelloDeveloper.java
        |
        | javac
        v
HelloDeveloper.class
        |
        | java / JVM
        v
Результат у консолі
```

Окремо письмово дай відповіді:

1. Чому `.class` не є звичайним текстовим Java-кодом?
2. Яку роль виконує JVM під час запуску?
3. Чому Java-програму можна запускати на різних операційних системах?
4. Чи входить компілятор `javac` до JVM?
5. Що станеться, якщо в програмі немає коректного методу `main`?

### Критерій завершення блоку

- Переглянуті всі заплановані уроки.
- Таблиця понять заповнена без копіювання визначень.
- Схема компіляції та запуску зрозуміла.
- Є письмові відповіді на 5 контрольних запитань.

---

## 3. Керована практика — 60 хвилин

### Завдання 3.1. Перевір інструменти

У терміналі, з кореня проєкту, виконай команди по черзі:

```bash
pwd
java -version
javac -version
mvn -version
git --version
git status
```

Запиши в `learning-journal.md`:

- поточну директорію;
- версію Java;
- версію `javac`;
- версію Maven;
- версію Git;
- назву активної Git-гілки;
- чи є в репозиторії незбережені зміни.

Важливо: у `pom.xml` зараз задано Java `25`. Порівняй це значення з версією,
яку показали `java -version` та `javac -version`. Сьогодні не змінюй
`pom.xml` лише заради збігу версій. Якщо версії несумісні, запиши це як
окреме спостереження.

### Завдання 3.2. Досліди Maven-проєкт

Знайди й відкрий:

- `pom.xml`;
- `src/main/java`;
- `src/test/java`;
- `target`.

У конспекті поясни призначення кожного елемента.

Потім знайди в `pom.xml`:

- `groupId`;
- `artifactId`;
- `version`;
- Java compiler source і target;
- test dependency.

Запиши їхні поточні значення. Поки що нічого в `pom.xml` не змінюй.

### Завдання 3.3. Повтори першу програму за курсом

В IntelliJ IDEA створи файл:

```text
src/main/java/daniil/dumshenko/HelloDeveloper.java
```

Початковий результат програми:

```text
Hello, Java Developer!
Day 1: Environment is ready.
```

Не копіюй готовий клас. Самостійно напиши:

- package declaration;
- class declaration;
- метод `main`;
- два виклики `System.out.println`.

Запусти клас через IntelliJ IDEA.

### Завдання 3.4. Зміни поведінку

Після успішного запуску:

1. Заміни текст першого рядка на привітання зі своїм ім'ям.
2. Додай третій рядок із метою навчання.
3. Навмисно видали одну крапку з комою.
4. Прочитай повідомлення компілятора й запиши його основну частину.
5. Виправ помилку та повторно запусти програму.

### Критерій завершення блоку

- Версії інструментів зафіксовані.
- Структура Maven-проєкту пояснена.
- `HelloDeveloper` запускається в IntelliJ IDEA.
- Одна syntax error створена, прочитана та виправлена свідомо.

---

## 4. Самостійне завдання — 120 хвилин

Цей блок виконай без перегляду рішення курсу. Документацію та власний
конспект використовувати можна. AI або готове рішення — лише після
щонайменше 30 хвилин самостійної спроби.

### Завдання 4.1. Розшир `HelloDeveloper`

Програма повинна вивести структуровану картку:

```text
================================
JAVA BACKEND LEARNING PROGRAM
================================
Student: <твоє ім'я>
Target role: Junior Java Backend Developer
Target country: Germany
Current day: 1 of 84
Today's topic: Java environment and execution
================================
```

Вимоги:

- кожен логічний рядок виводиться окремою командою;
- текст не повинен починатися або закінчуватися випадковими пробілами;
- результат має легко читатися в консолі;
- назва класу має точно збігатися з назвою файла;
- package має відповідати директорії.

### Завдання 4.2. Запусти програму з термінала через Maven

З кореня проєкту виконай:

```bash
mvn compile
```

Після успішної компіляції знайди:

```text
target/classes/daniil/dumshenko/HelloDeveloper.class
```

Запусти програму:

```bash
java -cp target/classes daniil.dumshenko.previousLessons.HelloDeveloper
```

Запиши:

- що створила команда `mvn compile`;
- навіщо використовується `-cp target/classes`;
- чому в команді запуску вказано повне ім'я
  `daniil.dumshenko.previousLessons.HelloDeveloper`.

### Завдання 4.3. Скомпілюй без Maven

Створи тимчасову директорію для результату компіляції:

```bash
mkdir -p /tmp/codewithmosh-day1
```

Скомпілюй клас:

```bash
javac -d /tmp/codewithmosh-day1 src/main/java/daniil/dumshenko/HelloDeveloper.java
```

Запусти його:

```bash
java -cp /tmp/codewithmosh-day1 daniil.dumshenko.previousLessons.HelloDeveloper
```

Порівняй:

- запуск через IntelliJ IDEA;
- компіляцію через Maven;
- ручну компіляцію через `javac`.

У конспекті напиши по 1-2 речення про те, що в кожному випадку відбувається
автоматично, а що ти робиш вручну.

### Завдання 4.4. Досліди bytecode

Переконайся, що `.class`-файл існує:

```bash
find target/classes -name 'HelloDeveloper.class'
```

Переглянь структуру скомпільованого класу:

```bash
javap -classpath target/classes daniil.dumshenko.previousLessons.HelloDeveloper
```

За бажанням переглянь інструкції bytecode:

```bash
javap -c -classpath target/classes daniil.dumshenko.previousLessons.HelloDeveloper
```

Не потрібно запам'ятовувати bytecode. Треба зрозуміти, що JVM виконує не
вихідний `.java`-файл безпосередньо.

Відповідай у конспекті:

1. Який файл є source code?
2. Який файл містить bytecode?
3. Яка команда компілює source code?
4. Яка команда запускає клас у JVM?
5. Чим Maven допоміг порівняно з ручним `javac`?

### Завдання 4.5. Перевір Git-стан

Виконай:

```bash
git status
git diff
git diff --stat
```

Зверни увагу: у репозиторії можуть уже бути зміни, які не стосуються Дня 1.
Не видаляй і не перезаписуй їх.

Визнач:

- які файли були змінені до початку сьогоднішньої роботи;
- які файли створені або змінені сьогодні;
- які саме рядки належать до сьогоднішнього завдання.

Підготуй для commit лише файли Дня 1:

```bash
git add TASKS.md learning-journal.md src/main/java/daniil/dumshenko/HelloDeveloper.java
git diff --cached
```

Уважно переглянь staged diff. Не додавай через `git add .` сторонні або
незавершені зміни.

Рекомендоване повідомлення commit:

```text
Complete day 1 Java environment exercises
```

Сам commit виконуй лише тоді, коли свідомо готовий зберегти ці зміни:

```bash
git commit -m "Complete day 1 Java environment exercises"
```

### Критерій завершення блоку

- Програма має весь необхідний вивід.
- Вона запускається через IntelliJ IDEA, Maven і ручні команди.
- `.class`-файл знайдений і переглянутий через `javap`.
- Різниця між трьома способами запуску пояснена.
- До commit підготовлені лише файли поточного дня.

---

## 5. Перевірка та рефакторинг — 30 хвилин

Автоматичні unit tests для `println` сьогодні не потрібні. Перевір результат
через запуск програми та аналіз коду.

### Завдання 5.1. Функціональна перевірка

Запусти програму й перевір:

1. Заголовок присутній.
2. Ім'я студента присутнє.
3. Цільова роль указана правильно.
4. Країна — Germany.
5. День — `1 of 84`.
6. Тема дня присутня.
7. Розділювачі мають однакову довжину.
8. Немає порожніх випадкових рядків.

### Завдання 5.2. Перевірка помилок

Навмисно виконай по одному разу, а потім виправ:

- запусти неправильне ім'я класу;
- прибери одну подвійні лапки в рядку;
- зміни регістр однієї літери в назві файла або класу.

Для кожного випадку запиши:

- команду або зміну;
- головний рядок помилки;
- причину;
- виправлення.

Не залишай навмисні помилки в кінцевому коді.

### Завдання 5.3. Рефакторинг

Переглянь `HelloDeveloper.java`:

- чи зрозуміла назва класу;
- чи правильний package;
- чи вирівняні відступи;
- чи немає закоментованого непотрібного коду;
- чи немає зайвих imports;
- чи кожен рядок виводу потрібний;
- чи немає орфографічних помилок.

Потім виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.HelloDeveloper
git diff --check
```

### Критерій завершення блоку

- Пройдені всі 8 функціональних перевірок.
- Досліджені 3 типові помилки.
- Після виправлень `mvn compile` завершується успішно.
- `git diff --check` не показує whitespace errors.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Повернися до стартової діагностики

Не видаляючи початкових відповідей, додай:

```markdown
## День 1. Уточнення після навчання
```

Повторно дай відповіді:

1. Що входить до JDK?
2. Яка різниця між JDK, JVM і JRE?
3. Як `.java` перетворюється на `.class`?
4. Що робить команда `java`?
5. Що Maven зробив за тебе під час `mvn compile`?

Порівняй відповіді зі стартовою діагностикою.

### Завдання 6.2. Заповни щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 1. Підсумок

### Що я сьогодні вивчив
- 
- 
- 

### Що я зробив самостійно
- 
- 

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що залишилося незрозумілим
- 

### Що потрібно повторити завтра
- 

### Фактичний час
- Стартова діагностика:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни вголос:

> Як код із файла `HelloDeveloper.java` перетворюється на результат у
> консолі та яку роль у цьому відіграють JDK, компілятор, bytecode і JVM?

Якщо пояснення переривається через прогалину, запиши цю прогалину до секції
`Що потрібно повторити завтра`.

---

## Фінальний checklist Дня 1

- [ ] Стартова діагностика виконана без підказок.
- [ ] Пройдено секцію Getting Started.
- [ ] Заповнена таблиця Java-понять.
- [ ] Перевірені Java, `javac`, Maven і Git.
- [ ] Досліджена структура Maven-проєкту.
- [ ] Створено `HelloDeveloper.java`.
- [ ] Програма запускається через IntelliJ IDEA.
- [ ] Програма компілюється через `mvn compile`.
- [ ] Програма запускається з `target/classes`.
- [ ] Програма скомпільована вручну через `javac`.
- [ ] `.class`-файл переглянутий через `javap`.
- [ ] Перевірено 8 вимог до консольного виводу.
- [ ] Досліджено й виправлено 3 навмисні помилки.
- [ ] Заповнений підсумок у `learning-journal.md`.
- [ ] Підготовлений або створений змістовний Git commit.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `HelloDeveloper` запускається трьома способами.
2. Ти можеш пояснити шлях `.java -> .class -> JVM -> результат`.
3. Усі обов'язкові пункти checklist виконані.
4. У Git не додані випадкові сторонні зміни.
5. У журналі записані труднощі, фактичний час і тема для повторення завтра.

Якщо хоча б один із перших чотирьох пунктів не виконаний, День 2 поки не
починай. Спочатку заверши або виправ завдання Дня 1.

---

# День 2. Змінні, primitive та reference types

## Допуск до Дня 2

Перед початком нового матеріалу закрий незавершені пункти Дня 1:

1. У `HelloDeveloper.java` зроби стандартну точку входу:

   ```java
   public static void main(String[] args)
   ```

2. Переконайся, що ім'я студента можна вивести зі `static main`. Обери
   найпростіший зрозумілий варіант і поясни, чому нестатичне поле не можна
   використати без створення об'єкта.
3. Виконай і запиши три перевірки помилок із завдання 5.2 Дня 1.
4. Додай підсумок Дня 1, фактичний час і тему для повторення.
5. Виконай:

   ```bash
   mvn compile
   java -cp target/classes daniil.dumshenko.previousLessons.HelloDeveloper
   git diff --check
   ```

Цей допуск не входить до 5 годин 30 хвилин Дня 2. Якщо будь-яка команда
завершується помилкою, спочатку знайди й виправ причину.

## Мета дня

Навчитися свідомо обирати тип змінної, розрізняти primitive та reference
types, використовувати константи, виконувати безпечне приведення типів і
передбачати результат операцій до запуску програми.

Наприкінці дня ти повинен:

- знати всі 8 primitive types та їхнє призначення;
- розуміти різницю між primitive value і reference;
- уміти оголошувати, ініціалізувати та змінювати змінні;
- використовувати `String`, escape sequences і `final`;
- розрізняти implicit та explicit casting;
- пояснювати overflow у `byte`;
- пояснювати, чому `double` не зберігає деякі десяткові дроби точно;
- самостійно написати й запустити `PersonalProfile`.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення і діагностика | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення і діагностика — 30 хвилин

### Завдання 1.1. Повтори День 1 без конспекту

У `learning-journal.md` додай:

```markdown
## День 2. Повторення
```

Письмово дай короткі відповіді:

1. Який шлях проходить `HelloDeveloper.java` до результату в консолі?
2. Чим JDK відрізняється від JVM?
3. Що створює `javac`?
4. Для чого команді `java` потрібен classpath?
5. Яка стандартна сигнатура точки входу Java-програми?

Не відкривай учорашній конспект, доки не запишеш усі відповіді. Після цього
перевір їх і познач виправлення окремо.

### Завдання 1.2. Початкова діагностика типів

До запуску коду передбач результат кожного фрагмента:

```java
int age = 25;
age = 26;
System.out.println(age);
```

```java
int result = 5 / 2;
System.out.println(result);
```

```java
double result = 5 / 2;
System.out.println(result);
```

```java
double result = 5.0 / 2;
System.out.println(result);
```

```java
char letter = 'A';
System.out.println(letter);
```

Для кожного прикладу запиши:

- прогноз;
- фактичний результат;
- пояснення, якщо прогноз був неправильним.

### Критерій завершення блоку

- Є 5 відповідей за матеріалом Дня 1.
- Є прогноз і фактичний результат для 5 фрагментів.
- Ти можеш пояснити різницю між `5 / 2` та `5.0 / 2`.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Types**, уроки 1-7.

Перед кожним уроком запиши одне запитання, на яке очікуєш отримати
відповідь. Після кожних 2-3 уроків закрий відео та відтвори 3-5 тез із
пам'яті. Приклади набирай вручну.

### Завдання 2.1. Таблиця primitive types

У `learning-journal.md` додай:

```markdown
## День 2. Конспект курсу
```

Заповни таблицю:

| Тип | Що зберігає | Приклад | Коли обрати |
|---|---|---|---|
| `byte` |  |  |  |
| `short` |  |  |  |
| `int` |  |  |  |
| `long` |  |  |  |
| `float` |  |  |  |
| `double` |  |  |  |
| `char` |  |  |  |
| `boolean` |  |  |  |

Числові межі не потрібно зубрити. Треба розуміти порядок розмірів і вміти
перевірити точні значення через `Byte.MIN_VALUE`, `Byte.MAX_VALUE` та
аналогічні константи wrapper classes.

### Завдання 2.2. Primitive і reference types

Своїми словами дай відповіді:

1. Що безпосередньо зберігає primitive-змінна?
2. Що концептуально зберігає reference-змінна?
3. Чому `String` не є primitive type?
4. Чим одинарні лапки відрізняються від подвійних?
5. Чому локальну змінну потрібно ініціалізувати перед читанням?
6. Що означає ключове слово `final`?
7. Чим declaration, initialization і assignment відрізняються між собою?

### Завдання 2.3. Літерали та escape sequences

Запиши по одному власному прикладу:

- `long` із суфіксом `L`;
- `float` із суфіксом `F`;
- читабельного числа з `_`;
- символу `char`;
- рядка з `\n`;
- рядка з `\t`;
- рядка з `\"`;
- Windows-шляху або іншого тексту з `\\`.

### Критерій завершення блоку

- Переглянуті уроки Types 1-7.
- Таблиця містить усі 8 primitive types.
- Є 7 письмових відповідей.
- Є 8 власних прикладів літералів та escape sequences.

---

## 3. Керована практика — 60 хвилин

Створи файл:

```text
src/main/java/daniil/dumshenko/TypeExperiments.java
```

Не копіюй усі приклади одним блоком. Додавай їх по одному, спочатку
передбачай результат, потім запускай і записуй висновок.

### Завдання 3.1. Десять присвоєнь

Створи щонайменше 10 прикладів, які разом показують:

1. declaration без assignment;
2. initialization;
3. повторний assignment;
4. присвоєння між двома `int`;
5. копіювання primitive value;
6. присвоєння одного `String` іншому;
7. конкатенацію `String`;
8. зміну `boolean`;
9. використання `char`;
10. використання `final`-константи.

Для прикладу копіювання primitive перевір:

```java
int first = 10;
int second = first;
first = 20;
```

До запуску запиши, яким буде `second` і чому.

### Завдання 3.2. П'ять прикладів casting

Реалізуй і поясни:

1. `int -> long`;
2. `int -> double`;
3. `double -> int`;
4. `int -> byte` у межах `byte`;
5. `int -> byte` поза межами `byte`.

Біля кожного прикладу в журналі вкажи:

- implicit чи explicit casting;
- прогнозований результат;
- фактичний результат;
- чи могла бути втрачена інформація.

### Завдання 3.3. Дві проблемні ситуації

**Overflow:**

```java
byte maximum = Byte.MAX_VALUE;
byte overflowed = (byte) (maximum + 1);
```

Перед запуском передбач значення `overflowed`, потім поясни результат.

**Точність `double`:**

```java
double sum = 0.1 + 0.2;
System.out.println(sum);
System.out.println(sum == 0.3);
```

Поясни, чому результат може відрізнятися від математичного очікування.
Запиши, чому для точних грошових обчислень не слід покладатися на `double`.
`BigDecimal` сьогодні використовувати не потрібно.

### Критерій завершення блоку

- `TypeExperiments` компілюється та запускається.
- Є 10 прикладів присвоєння.
- Є 5 прикладів casting із прогнозами.
- Overflow і проблема точності `double` відтворені та пояснені.

---

## 4. Самостійне завдання — 120 хвилин

Створи файл:

```text
src/main/java/daniil/dumshenko/PersonalProfile.java
```

Перші 45 хвилин працюй без AI та готового рішення. Можна використовувати
власний конспект і офіційну документацію Java.

### Завдання 4.1. Дані профілю

Програма повинна містити осмислені змінні для:

- імені;
- прізвища;
- віку;
- зросту в метрах;
- ваги в кілограмах;
- першої літери імені;
- статусу студента;
- кількості завершених навчальних днів;
- приблизної кількості годин навчання;
- цільової ролі;
- країни;
- улюбленої великої числової величини або населення міста/країни.

Використай у програмі всі 8 primitive types щонайменше по одному разу та
мінімум два значення типу `String`. Тип кожної змінної повинен мати
зрозуміле обґрунтування.

Не використовуй особисті дані, які не хочеш зберігати в Git.

### Завдання 4.2. Константи без magic numbers

Оголоси щонайменше три константи через `static final`, наприклад:

- кількість місяців у році;
- назву навчальної програми;
- загальну кількість днів програми;
- коефіцієнт для простого обчислення.

Не називай константи `NUMBER`, `VALUE` або `DATA`. Назва повинна пояснювати
значення.

### Завдання 4.3. Форматований вивід

Виведи профіль як читабельну картку. Обов'язково використай:

- `\n`;
- `\t`;
- лапки всередині рядка через `\"`;
- один зворотний слеш через `\\`;
- конкатенацію рядків;
- щонайменше одне просте обчислення зі змінними.

Приклад структури, але не готовий текст:

```text
================================
PERSONAL PROFILE
================================
Name: ...
Target role: ...
Learning progress:
    Completed days: ...
    Remaining days: ...
Quote: "..."
Example path: C:\...
================================
```

### Завдання 4.4. Пояснення вибору типів

У журналі створи таблицю:

| Змінна | Обраний тип | Чому цей тип |
|---|---|---|
|  |  |  |

Додай усі основні змінні `PersonalProfile`. Окремо відповідай:

1. Які змінні є primitive?
2. Які змінні є reference?
3. Які значення не повинні змінюватися?
4. Де в програмі можливе переповнення або втрата точності?
5. Чи є в коді число або текст, який краще зробити константою?

### Завдання 4.5. Запуск двома способами

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.PersonalProfile
```

Потім скомпілюй клас вручну:

```bash
mkdir -p /tmp/codewithmosh-day2
javac -d /tmp/codewithmosh-day2 src/main/java/daniil/dumshenko/PersonalProfile.java
java -cp /tmp/codewithmosh-day2 daniil.dumshenko.previousLessons.PersonalProfile
```

Обидва способи повинні давати однаковий результат.

### Критерій завершення блоку

- `PersonalProfile` використовує всі 8 primitive types і `String`.
- Є щонайменше 3 змістовні константи.
- Використані 4 заплановані escape sequences.
- У коді немає незрозумілих magic numbers.
- Вибір типів пояснений у журналі.
- Програма запускається через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Перевір `PersonalProfile`:

1. Усі обов'язкові поля присутні.
2. Усі 8 primitive types реально використовуються.
3. `String` використовується як reference type.
4. Константи оголошені через `static final`.
5. Назви констант написані у `UPPER_SNAKE_CASE`.
6. Результат простого обчислення правильний.
7. Escape sequences відображаються очікувано.
8. Вивід не містить випадкових пробілів і порожніх рядків.
9. Назви змінних пояснюють їхнє призначення.
10. Немає закоментованого непотрібного коду.

### Завдання 5.2. Перевірка compiler errors

По черзі створи, прочитай і виправ три помилки:

1. присвой `double` змінній `int` без explicit cast;
2. присвой текст у подвійних лапках змінній `char`;
3. спробуй повторно присвоїти значення `final`-змінній.

Для кожної запиши:

- зміну в коді;
- головний рядок compiler error;
- причину;
- виправлення.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Фінальні команди

```bash
mvn compile
java -cp target/classes daniil.dumshenko.previousLessons.TypeExperiments
java -cp target/classes daniil.dumshenko.previousLessons.PersonalProfile
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Виконані всі 10 функціональних перевірок.
- Досліджені та виправлені 3 compiler errors.
- Обидва класи запускаються після чистої компіляції.
- `git diff --check` не показує whitespace errors.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Назви 8 primitive types.
2. Чим primitive type відрізняється від reference type?
3. Чому `String` пишеться з великої літери?
4. Коли Java виконує implicit casting?
5. Чим explicit casting може бути небезпечним?
6. Чому `byte` може перейти з `127` до `-128`?
7. Чому `0.1 + 0.2` може не дорівнювати точно `0.3`?
8. Для чого використовують `final`?
9. Чому magic numbers ускладнюють підтримку коду?
10. Який тип ти обереш для грошей у майбутньому та чому не `double`?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 2. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і діагностика:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни:

> Як Java зберігає primitive та reference values, як відбувається casting і
> чому неправильний вибір числового типу може призвести до overflow або
> втрати точності?

Якщо пояснення переривається, запиши прогалину до тем на повторення.

---

## Фінальний checklist Дня 2

- [ ] Закритий допуск за День 1.
- [ ] Виконане повторення без конспекту.
- [ ] Пройдені уроки Types 1-7.
- [ ] Заповнена таблиця 8 primitive types.
- [ ] Пояснена різниця між primitive та reference types.
- [ ] Створено `TypeExperiments.java`.
- [ ] Виконано 10 прикладів присвоєння.
- [ ] Виконано 5 прикладів casting із прогнозами.
- [ ] Відтворено й пояснено overflow у `byte`.
- [ ] Відтворено й пояснено проблему точності `double`.
- [ ] Створено `PersonalProfile.java`.
- [ ] У `PersonalProfile` використані всі primitive types і `String`.
- [ ] Використано щонайменше 3 константи без magic numbers.
- [ ] Програма запускається через Maven і ручний `javac`.
- [ ] Виконані 10 функціональних перевірок.
- [ ] Досліджені та виправлені 3 compiler errors.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Зміни переглянуті через Git.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `TypeExperiments` і `PersonalProfile` компілюються та запускаються.
2. Ти можеш обґрунтувати тип кожної основної змінної.
3. Ти можеш пояснити casting, overflow і неточність `double`.
4. Усі обов'язкові пункти checklist виконані.
5. У журналі записані прогнози, помилки, фактичний час і тема для повторення.
6. У Git не підготовлені випадкові сторонні зміни.

---

# День 3. Масиви, вирази та `Math`

## Допуск до Дня 3

Перед початком нового матеріалу переконайся, що День 2 справді завершений:

1. `TypeExperiments.java` і `PersonalProfile.java` компілюються.
2. У `learning-journal.md` є підсумок Дня 2, фактичний час і тема для
   повторення.
3. Ти можеш без конспекту пояснити:
   - 8 primitive types;
   - різницю між primitive та reference types;
   - implicit і explicit casting;
   - overflow у `byte`;
   - чому `double` не підходить для точних грошових розрахунків.
4. Виконай:

   ```bash
   mvn compile
   java -cp target/classes daniil.dumshenko.previousLessons.TypeExperiments
   java -cp target/classes daniil.dumshenko.previousLessons.PersonalProfile
   git diff --check
   ```

Якщо будь-яка команда завершується помилкою, спочатку виправ День 2. Не
починай новий матеріал, поки не зрозумів причину помилки.

## Мета дня

Навчитися працювати з одновимірними та багатовимірними масивами, передбачати
результат арифметичних виразів, використовувати `Math`, округлення та
форматування чисел у практичних задачах.

Наприкінці дня ти повинен:

- створювати та ініціалізувати масиви;
- читати й змінювати елементи масиву за індексом;
- пояснювати, чому індексація починається з `0`;
- розуміти `ArrayIndexOutOfBoundsException`;
- створювати та виводити матрицю 3x3;
- самостійно рахувати `min`, `max`, `sum` і `average`;
- розвертати масив без готового методу;
- передбачати порядок виконання арифметичних операцій;
- використовувати `Math.round`, `Math.ceil`, `Math.floor`, `Math.max`,
  `Math.min` і `Math.random`;
- форматувати числа через `NumberFormat`;
- написати й запустити `ArrayStatistics` і `CurrencyConverter`.

## Розклад дня

| Блок | Час |
|---|---:|
| 1. Повторення і діагностика | 30 хв |
| 2. Блок курсу | 90 хв |
| 3. Керована практика | 60 хв |
| 4. Самостійне завдання | 120 хв |
| 5. Перевірка та рефакторинг | 30 хв |
| 6. Підсумок дня | 20 хв |
| **Разом** | **5 год 30 хв** |

---

## 1. Повторення і діагностика — 30 хвилин

### Завдання 1.1. Повтори День 2 без конспекту

У `learning-journal.md` додай:

```markdown
## День 3. Повторення
```

Письмово дай короткі відповіді:

1. Назви 8 primitive types.
2. Чим primitive value відрізняється від reference value?
3. Що означає `final`?
4. Коли Java дозволяє implicit casting?
5. Чому explicit casting може втратити частину даних?
6. Що відбувається при `byte` overflow?
7. Чому `0.1 + 0.2` не завжди дає рівно `0.3`?

Спочатку відповідай із пам'яті. Потім перевір конспект і допиши уточнення
окремим підпунктом `Уточнення після перевірки`.

### Завдання 1.2. Передбач результат виразів

До запуску коду передбач результат кожного фрагмента:

```java
int result = 10 + 3 * 2;
System.out.println(result);
```

```java
int result = (10 + 3) * 2;
System.out.println(result);
```

```java
double result = 10 / 4;
System.out.println(result);
```

```java
double result = 10.0 / 4;
System.out.println(result);
```

```java
int x = 5;
int y = ++x * 2;
System.out.println(x);
System.out.println(y);
```

Для кожного прикладу запиши:

- прогноз;
- фактичний результат;
- пояснення, якщо прогноз був неправильним.

### Критерій завершення блоку

- Є 7 відповідей за матеріалом Дня 2.
- Є прогноз і фактичний результат для 5 фрагментів.
- Ти можеш пояснити різницю між `10 / 4` та `10.0 / 4`.
- Ти можеш пояснити роль дужок у порядку операцій.

---

## 2. Блок курсу — 90 хвилин

### Матеріал курсу

Пройди секцію **Ultimate Java Part 1: Fundamentals -> Types**, уроки 8-15:

1. Arrays.
2. Multi-dimensional Arrays.
3. Constants.
4. Arithmetic Expressions.
5. Order of Operations.
6. Casting.
7. The Math Class.
8. Formatting Numbers.

Уроки про constants і casting сьогодні частково повторюють День 2. Під час
повтору не перемотуй автоматично: шукай деталі, які вчора були незрозумілі.

### Як працювати з відео

1. Перед кожним уроком запиши одне запитання.
2. Після уроків 8-9 зупинись і самостійно створи маленький масив.
3. Після уроків 11-13 зупинись і розв'яжи 3 вирази вручну.
4. Після уроків 14-15 закрий відео й напиши власний приклад із `Math` та
   форматуванням числа.
5. Усі приклади набирай вручну, не копіюй із відео.

### Завдання 2.1. Конспект про масиви

У `learning-journal.md` додай:

```markdown
## День 3. Конспект курсу
```

Своїми словами відповідай:

1. Що таке масив?
2. Чому масив має фіксований розмір?
3. Що означає індекс елемента?
4. Чому перший елемент має індекс `0`?
5. Що повертає `array.length`?
6. Що станеться при зверненні до неіснуючого індексу?
7. Чим `int[] numbers = new int[5]` відрізняється від
   `int[] numbers = {1, 2, 3, 4, 5}`?
8. Як у Java виглядає двовимірний масив?

### Завдання 2.2. Таблиця `Math` і форматування

Заповни таблицю:

| Інструмент | Що робить | Власний приклад | Результат |
|---|---|---|---|
| `Math.round` |  |  |  |
| `Math.ceil` |  |  |  |
| `Math.floor` |  |  |  |
| `Math.max` |  |  |  |
| `Math.min` |  |  |  |
| `Math.random` |  |  |  |
| `NumberFormat.getCurrencyInstance()` |  |  |  |
| `NumberFormat.getPercentInstance()` |  |  |  |

### Завдання 2.3. Порядок операцій

Розв'яжи письмово до запуску:

```java
int a = 10 + 2 * 3;
int b = (10 + 2) * 3;
int c = 20 / 4 + 6;
int d = 20 / (4 + 6);
double e = 5 + 2.0 * 4 / 3;
```

Для кожного виразу запиши:

- порядок виконання кроків;
- прогнозований результат;
- фактичний результат після запуску.

### Критерій завершення блоку

- Переглянуті уроки Types 8-15.
- Є 8 відповідей про масиви.
- Таблиця `Math` і `NumberFormat` заповнена.
- Є письмовий розбір 5 виразів на порядок операцій.

---

## 3. Керована практика — 60 хвилин

Створи файл:

```text
src/main/java/daniil/dumshenko/ArrayExperiments.java
```

Додавай приклади поступово. Перед запуском кожного блоку записуй прогноз у
журнал.

### Завдання 3.1. Одновимірні масиви

Реалізуй приклади:

1. Створи `int[] scores = {78, 92, 64, 100, 85}`.
2. Виведи перший, третій і останній елемент.
3. Зміни другий елемент на нове значення.
4. Виведи довжину масиву.
5. Виведи весь масив через `Arrays.toString`.
6. Створи `double[] prices` на 4 елементи через `new double[4]`.
7. Заповни `prices` вручну через індекси.
8. Виведи `prices` через `Arrays.toString`.

Письмово поясни, чому звичайний `System.out.println(scores)` не виводить
масив як список чисел.

### Завдання 3.2. Помилка індексу

Навмисно звернись до індексу, якого не існує, наприклад:

```java
System.out.println(scores[5]);
```

Запиши:

- повну назву exception;
- який індекс був неправильним;
- яка фактична довжина масиву;
- чому останній допустимий індекс дорівнює `length - 1`.

Після цього виправ код. У фінальній версії програма не повинна падати.

### Завдання 3.3. Матриця 3x3

Створи `int[][] matrix` із числами від 1 до 9:

```text
1 2 3
4 5 6
7 8 9
```

Виведи її двома способами:

1. Через `Arrays.deepToString(matrix)`.
2. Як три окремі рядки, щоб результат був схожий на матрицю.

Поясни:

- що означає `matrix.length`;
- що означає `matrix[0].length`;
- чим `Arrays.toString` відрізняється від `Arrays.deepToString` для
  двовимірного масиву.

### Завдання 3.4. `Math` на практиці

У тому ж класі створи приклади:

1. Знайди більше з двох чисел через `Math.max`.
2. Знайди менше з двох чисел через `Math.min`.
3. Округли `14.6` через `round`, `ceil` і `floor`.
4. Згенеруй випадкове число від `0` до `1`.
5. Згенеруй випадкове ціле число від `1` до `100`.

Для випадкового числа від `1` до `100` письмово поясни формулу.

### Критерій завершення блоку

- `ArrayExperiments` компілюється та запускається.
- Є приклади створення масиву через literal і через `new`.
- Є приклад читання, зміни та виводу елементів.
- `ArrayIndexOutOfBoundsException` свідомо створений, пояснений і виправлений.
- Матриця 3x3 виведена двома способами.
- Є робочі приклади `Math`.

---

## 4. Самостійне завдання — 120 хвилин

Перші 45 хвилин працюй без AI та готового рішення. Можна використовувати
власний конспект і офіційну документацію Java.

### Завдання 4.1. `ArrayStatistics`

Створи файл:

```text
src/main/java/daniil/dumshenko/ArrayStatistics.java
```

Програма повинна працювати з масивом:

```java
int[] transactions = {120, -45, 300, -80, 50, -20, 700};
```

Реалізуй без готових методів для статистики:

1. `sum` усіх елементів;
2. `min`;
3. `max`;
4. `average`;
5. кількість додатних значень;
6. кількість від'ємних значень;
7. окрему суму доходів;
8. окрему суму витрат;
9. копію масиву у зворотному порядку;
10. вивід оригінального та розвернутого масивів.

Важливо:

- `average` має бути `double`, не `int`;
- не використовуй `Collections`, streams або готовий reverse;
- для виводу масивів використовуй `Arrays.toString`;
- назви змінних мають пояснювати зміст, а не механіку.

Очікувані ідеї для перевірки:

- у масиві є і доходи, і витрати;
- витрати можна рахувати як суму від'ємних чисел або як абсолютну суму, але
  обраний варіант треба явно підписати у виводі;
- розвернутий масив не повинен змінювати оригінальний.

### Завдання 4.2. Перевір крайні випадки для `ArrayStatistics`

Тимчасово перевір програму на додаткових масивах:

```java
int[] onlyIncome = {100, 200, 300};
int[] onlyExpenses = {-10, -20, -30};
int[] singleValue = {42};
int[] withZero = {0, 100, -50};
```

Не обов'язково залишати всі ці масиви активними у фінальному коді. Але в
журналі запиши для кожного:

- прогноз `sum`, `min`, `max`, `average`;
- фактичний результат;
- чи правильно працює reverse.

Порожній масив сьогодні можна не підтримувати в коді, але письмово відповідай:

> Що станеться з `min`, `max` і `average`, якщо масив порожній?

### Завдання 4.3. `CurrencyConverter`

Створи файл:

```text
src/main/java/daniil/dumshenko/CurrencyConverter.java
```

Програма повинна конвертувати суму з EUR у USD, GBP і UAH.

Вимоги:

- сума в EUR зберігається у змінній;
- курси валют винесені в `static final` константи;
- результат рахується через арифметичні вирази;
- результат виводиться у форматі валюти через `NumberFormat`;
- додатково виведи один відсоток через `NumberFormat.getPercentInstance()`;
- використай `Math.round` або `Math.floor` в окремому прикладі округлення;
- не використовуй введення з клавіатури сьогодні.

Приклад структури виводу:

```text
================================
CURRENCY CONVERTER
================================
Base amount: ...
EUR to USD: ...
EUR to GBP: ...
EUR to UAH: ...
Example fee: ...
Rounded amount: ...
================================
```

Курси валют можуть бути навчальними, не актуальними ринковими даними. У
виводі або коментарі вкажи, що це `training rates`.

### Завдання 4.4. Пояснення до самостійної роботи

У `learning-journal.md` додай:

```markdown
## День 3. Самостійне завдання
```

Відповідай:

1. Чому для `average` потрібно хоча б одне `double`-значення в обчисленні?
2. Чому останній індекс масиву — це `length - 1`?
3. Як працює алгоритм розвороту масиву?
4. Чому оригінальний масив не повинен змінюватися під час створення reversed
   copy?
5. Які значення краще винести в константи в `CurrencyConverter`?
6. Чому `NumberFormat` краще за ручне додавання символу валюти до рядка?
7. Де в цих задачах може виникнути integer division?

### Завдання 4.5. Запуск

З кореня проєкту виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.ArrayExperiments
java -cp target/classes daniil.dumshenko.ArrayStatistics
java -cp target/classes daniil.dumshenko.CurrencyConverter
```

Потім скомпілюй два самостійні класи вручну:

```bash
mkdir -p /tmp/codewithmosh-day3
javac -d /tmp/codewithmosh-day3 src/main/java/daniil/dumshenko/ArrayStatistics.java src/main/java/daniil/dumshenko/CurrencyConverter.java
java -cp /tmp/codewithmosh-day3 daniil.dumshenko.ArrayStatistics
java -cp /tmp/codewithmosh-day3 daniil.dumshenko.CurrencyConverter
```

### Критерій завершення блоку

- `ArrayStatistics` рахує `sum`, `min`, `max`, `average`.
- Масив розвертається без готового reverse.
- Оригінальний масив не змінюється під час розвороту.
- Перевірені щонайменше 4 крайні випадки.
- `CurrencyConverter` використовує константи для курсів.
- Валюта й відсоток форматуються через `NumberFormat`.
- Обидві програми запускаються через Maven і ручний `javac`.

---

## 5. Перевірка та рефакторинг — 30 хвилин

### Завдання 5.1. Функціональна перевірка

Перевір `ArrayStatistics`:

1. `sum` правильний.
2. `min` правильний.
3. `max` правильний.
4. `average` не обрізається до `int`.
5. Додатні та від'ємні значення рахуються правильно.
6. Доходи та витрати підписані зрозуміло.
7. Reversed copy має правильний порядок.
8. Оригінальний масив після reverse не змінився.

Перевір `CurrencyConverter`:

1. Курси валют оголошені як `static final`.
2. У виводі зрозуміло, яка сума базова.
3. USD, GBP і UAH рахуються окремо.
4. Валютні значення форматуються через `NumberFormat`.
5. Відсоток форматуються через `NumberFormat`.
6. Є приклад округлення через `Math`.
7. У виводі немає випадкових пробілів і порожніх рядків.

### Завдання 5.2. Перевірка помилок

По черзі створи, прочитай і виправ три помилки:

1. звернення до індексу за межами масиву;
2. спроба змінити значення `static final` константи;
3. неправильний import або відсутній import для `Arrays` чи `NumberFormat`.

Для кожної запиши:

- зміну в коді;
- головний рядок помилки або exception;
- причину;
- виправлення.

Не залишай навмисні помилки у фінальному коді.

### Завдання 5.3. Рефакторинг

Переглянь код:

- чи немає дублювання однакових чисел, які треба винести в константи;
- чи зрозумілі назви змінних;
- чи немає закоментованого непотрібного коду;
- чи imports справді використовуються;
- чи форматування однакове в усіх класах;
- чи довгі рядки можна розбити без втрати читабельності;
- чи всі результати у виводі підписані так, щоб не треба було вгадувати.

Потім виконай:

```bash
mvn compile
java -cp target/classes daniil.dumshenko.ArrayExperiments
java -cp target/classes daniil.dumshenko.ArrayStatistics
java -cp target/classes daniil.dumshenko.CurrencyConverter
git diff --check
git status
git diff --stat
```

### Критерій завершення блоку

- Виконані всі функціональні перевірки.
- Досліджені та виправлені 3 помилки.
- Усі три класи запускаються після чистої компіляції.
- `git diff --check` не показує whitespace errors.
- У Git видно лише очікувані зміни.

---

## 6. Підсумок дня — 20 хвилин

### Завдання 6.1. Контрольні запитання

Без конспекту письмово відповідай:

1. Що таке масив?
2. Чому масив має фіксований розмір?
3. Чому перший індекс дорівнює `0`?
4. Що означає `array.length`?
5. Чому останній індекс — `array.length - 1`?
6. Коли виникає `ArrayIndexOutOfBoundsException`?
7. Як створити двовимірний масив?
8. Чим `Arrays.toString` відрізняється від `Arrays.deepToString`?
9. Як вручну знайти `min` і `max` у масиві?
10. Як розвернути масив без готового методу?
11. Чому `average` може бути неправильним при integer division?
12. Для чого використовують `Math.round`, `ceil` і `floor`?
13. Для чого використовують `NumberFormat`?

### Завдання 6.2. Щоденний звіт

Додай у `learning-journal.md`:

```markdown
## День 3. Підсумок

### Що я сьогодні вивчив
-
-
-

### Що я зробив самостійно
-
-

### Де мій прогноз не збігся з результатом
-

### Які помилки я зустрів
- Помилка:
  Причина:
  Виправлення:

### Що залишилося незрозумілим
-

### Що потрібно повторити завтра
-

### Фактичний час
- Повторення і діагностика:
- Курс:
- Керована практика:
- Самостійне завдання:
- Перевірка та рефакторинг:
- Підсумок:
- Разом:
```

### Завдання 6.3. Усне пояснення

Без конспекту протягом 3-5 хвилин поясни:

> Як у Java працюють масиви, чому індекси починаються з нуля, як вручну
> порахувати статистику масиву та чому форматування чисел краще робити через
> спеціальні інструменти, а не через ручну конкатенацію рядків?

Якщо пояснення переривається, запиши прогалину до тем на повторення.

---

## Фінальний checklist Дня 3

- [ ] Закритий допуск за День 2.
- [ ] Виконане повторення без конспекту.
- [ ] Пройдені уроки Types 8-15.
- [ ] Заповнені відповіді про масиви.
- [ ] Заповнена таблиця `Math` і `NumberFormat`.
- [ ] Розв'язано 5 виразів на порядок операцій.
- [ ] Створено `ArrayExperiments.java`.
- [ ] Створено та змінено одновимірний масив.
- [ ] Свідомо створено, пояснено й виправлено `ArrayIndexOutOfBoundsException`.
- [ ] Створено та виведено матрицю 3x3.
- [ ] Виконані практичні приклади з `Math`.
- [ ] Створено `ArrayStatistics.java`.
- [ ] Пораховано `sum`, `min`, `max`, `average`.
- [ ] Пораховано доходи, витрати та кількість додатних/від'ємних значень.
- [ ] Масив розвернуто без готового методу.
- [ ] Перевірено 4 крайні випадки для статистики.
- [ ] Створено `CurrencyConverter.java`.
- [ ] Курси валют винесені в `static final` константи.
- [ ] Валюта й відсоток форматуються через `NumberFormat`.
- [ ] Усі класи запускаються через Maven.
- [ ] Самостійні класи запускаються через ручний `javac`.
- [ ] Досліджені та виправлені 3 помилки.
- [ ] Заповнений підсумок із фактичним часом.
- [ ] Зміни переглянуті через Git.
- [ ] Тему дня пояснено вголос без конспекту.

## День вважається завершеним, якщо

1. `ArrayExperiments`, `ArrayStatistics` і `CurrencyConverter` компілюються та
   запускаються.
2. Ти можеш пояснити індексацію масивів і причину `ArrayIndexOutOfBoundsException`.
3. Ти можеш вручну знайти `min`, `max`, `sum`, `average` і розвернути масив.
4. Ти можеш пояснити порядок операцій у виразах із дужками, множенням,
   діленням, додаванням і casting.
5. Ти розумієш базову різницю між округленням через `Math` і форматуванням
   через `NumberFormat`.
6. У журналі записані прогнози, помилки, фактичний час і тема для повторення.
7. У Git не підготовлені випадкові сторонні зміни.
